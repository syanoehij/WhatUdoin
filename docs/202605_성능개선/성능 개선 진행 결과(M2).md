# 성능 개선 진행 결과 (M2)

`성능 개선 todo.md` M2 섹션 step별 4종 기록. 본 파일은 M2 진입 게이트 및 이후 조건부 운영 구조 변경 step의 증거 인덱스를 누적한다.

기록 형식 (4종):
- 변경: 어떤 함수/파일에 무엇이 추가/수정됐는지 1~2줄.
- 증거: exit criteria 검증 파일 경로/grep 결과/테스트명.
- 회귀: 통과한 회귀 테스트명 또는 회귀 영향 없음 사유.
- 다음 step 영향: 후속 step의 가정/입력에 영향이 가는 사실 1줄.

---

## M2-0 완료 (2026-05-10) — M2 진입 게이트 평가

- **변경**: 운영 코드 변경 없음. M2-0 측정 자동화를 위해 `_workspace/perf/scripts/m2_0_gate_probe.py`를 추가하고, snapshot → seed → 8443 서버 기동 → 50 SSE 연결 → 서버 재시작 → cleanup 흐름으로 게이트를 측정했다.
- **증거**: `_workspace/perf/baseline_2026-05-09/m2_0_002210/m2_0_gate_report.md` / `_workspace/perf/baseline_2026-05-09/m2_0_002210/m2_0_gate_report.json` / `_workspace/perf/baseline_2026-05-09/m2_0_002210/m2_0_api_summary.csv`. 측정값: baseline_no_sse `/api/events` p95 89.8ms, with_50_sse p95 89.3ms, restart_reconnect_window p95 88.3ms, 각 API probe failure 0건. SSE initial connect 50/50 in 619.1ms, restart reconnect 50/50 in 216.4ms.
- **회귀**: `m2_0_gate_probe.py` py_compile 통과. test_perf fixture cleanup 확인 — users 0, sessions 0, `session_cookies.json` 없음. 측정 서버 종료 확인 — 8000/8443 LISTEN 없음(TIME_WAIT만 잔존). API probe 3개 scenario 모두 failure 0건.
- **다음 step 영향**: 개발 PC 측정만 보면 **NO-GO**. 단, 운영 대상 PC가 개발 PC 대비 낮은 사양이고 HDD + NASCA 사내 보안 시스템으로 I/O 성능이 더 낮다는 운영 환경 차이가 확인되어, M2는 **target-risk override**로 진입한다. 본 probe는 조회 중심이며 upload pressure는 포함하지 않았으므로, M2 구현 중에도 회사 반입 판단은 느린 PC/운영 유사 환경에서 재측정해야 한다.

## M2-0 운영 환경 override (2026-05-10)

- **변경**: 운영 코드 변경 없음. M2-0 판정을 "개발 PC 조회/SSE/restart probe 기준 NO-GO"로 보존하되, 실제 회사 서버 사양/보안 I/O 조건을 반영해 M2 진입을 허용하는 운영 리스크 override를 문서화했다.
- **증거**: 사용자 운영 조건 확인 — 회사 서버 PC는 현재 측정 PC 대비 약 4배 낮은 사양이며, SSD가 아닌 HDD + NASCA 사내 보안 시스템 영향으로 I/O 성능이 더 낮다. `성능 개선 계획.md` §15 판단 우선순위는 동일 환경 before/after와 환경 메타데이터를 중시하므로, 개발 PC NO-GO를 회사 서버 NO-GO로 일반화하지 않는다.
- **회귀**: 문서 판정 보정만 수행, 코드 변경 0.
- **다음 step 영향**: M2-1 진입 가능. 이후 모든 M2 측정/회사 반입 판단은 개발 PC 단일 측정이 아니라 회사 서버 또는 운영 유사 느린 I/O 환경에서 재검증해야 한다.

---

## M2-1 완료 (2026-05-10) — 외부 포트 8000/8443 listener 소유자 결정

- **변경**: 운영 코드 변경 없음. M2의 외부 포트 소유자는 **Tray/Supervisor/Front Router**로 결정했다. 기존 main app은 장기 목표에서 외부 8000/8443 listener를 직접 소유하지 않고, 내부 Web API service로 내려간다.
- **증거**: `성능 개선 계획.md` §13 장기 운영 모델은 Tray/Supervisor/Front Router가 기존 외부 8000/8443 포트, TLS/인증서, 라우팅, service lifecycle, watchdog을 소유하는 구조를 권장한다. `성능 개선 계획.md` §16 M2는 rev14 장기 모델 채택 시 Tray/Supervisor/Front Router가 외부 listener를 소유하고 Web API/SSE/MCP 내부 service로 라우팅한다고 명시한다.
- **회귀**: 결정 step이므로 코드/서버 변경 없음. 문서 정합성만 갱신한다.
- **다음 step 영향**: M2-2는 main app 소유 + 별도 LAN 포트 분기보다, **라우터 소유 + `/api/stream`만 SSE service로 라우팅하는 (a) 옵션**을 우선 검토한다. 이 결정은 M2-3 canonical URL, M2-4 HTTP fallback, M2-10 Front Router 최소 구현, M2-13 TRUSTED_PROXY/외부 직접 접근 차단의 선행 가정이 된다.

---

## M2-2 완료 (2026-05-10) — 사용자-facing SSE 접근 경로 결정

- **변경**: 운영 코드 변경 없음. 사용자-facing SSE 접근 경로는 **(a) Tray/Supervisor/Front Router가 외부 `/api/stream`을 SSE service로 라우팅**하는 방식으로 결정했다. 브라우저는 기존 origin의 상대 경로 `/api/stream`을 계속 사용하고, 라우터가 내부 loopback SSE service로 reverse proxy한다.
- **증거**: `성능 개선 계획.md` §13 "사용자-facing SSE 접근 경로"는 (a)를 main app 재시작 시 SSE 연결 유지 목표를 가장 잘 달성하는 경로로 설명한다. 같은 문서 §16 M2는 rev14 장기 모델의 권장안을 (a) 트레이/프론트 라우터가 `/api/stream`만 SSE service로 라우팅하는 구조라고 명시한다. M2-1에서 외부 포트 소유자를 Tray/Supervisor/Front Router로 결정했으므로 (a)가 라우팅 구조와 정합한다.
- **회귀**: 결정 step이므로 코드/서버 변경 없음. (b) 별도 LAN 포트 직접 공개는 CORS, 쿠키/세션, HTTPS SAN, CSP 비용이 커서 비채택. (c) main app proxy는 main app 재시작 시 SSE도 같이 끊겨 M2 핵심 목표가 약해져 비채택.
- **다음 step 영향**: M2-3은 외부 canonical URL을 라우터/슈퍼바이저가 단일 소유하는 방향으로 결정해야 한다. M2-10 구현 시 외부 `/api/stream`은 SSE service로, `/`와 `/api/*` 일반 요청은 Web API service로 보낸다. M2-20 종료 검증에서는 main app/Web API service 재시작 중 SSE 연결 유지 또는 알림 누락 0건을 (a) 기준으로 측정한다.

---

## M2-3 완료 (2026-05-10) — 외부 canonical URL 정책 결정

- **변경**: `app.py`에 `_public_base_url()` canonical helper를 추가했다. 정책은 **사용 중인 PC의 LAN IP 기반 origin 유지**다. M2 Supervisor/Front Router는 시작 시 현재 PC 외부 접근 IP를 계산해 `WHATUDOIN_PUBLIC_BASE_URL=https://<PC-IP>:8443`로 Web API/SSE service에 주입하고, Web API는 그 host로 HTTPS/HTTP origin을 만든다. env가 없으면 기존 단일 프로세스 운영 호환을 위해 request host를 fallback으로 쓴다. `/settings/mcp`, 인증서 HTTPS probe, AVR HTTP fallback, 체크/문서 링크 복사 UI가 helper 또는 `_ctx()`의 `public_https_base`/`public_http_base`를 사용하도록 바꿨다.
- **증거**: `app.py:387` `_public_base_url()`, `app.py:551` `_ctx()` public base 주입, `app.py:739` `/settings/mcp`, `app.py:4213` `_http_avr_url()`, `templates/base.html:15` HTTPS probe, `templates/check.html:1809`, `templates/check_editor.html:424`, `templates/doc_editor.html:618`. 검증 명령: `rg -n "request\.base_url|base_url\.hostname|location\.hostname.*8000|loc\.hostname.*8000" app.py templates static/js` 결과 0건. 남은 `request.url.hostname`은 `app.py:394` canonical helper의 단일 fallback 지점이다.
- **회귀**: `python -m py_compile app.py` 통과, `python -c "import app; print('OK')"` 통과. `WHATUDOIN_PUBLIC_BASE_URL=https://192.168.0.18:8443` 설정 시 `_public_base_url(req, 'https')` → `https://192.168.0.18:8443`, `_public_base_url(req, 'http')` → `http://192.168.0.18:8000` 확인.
- **다음 step 영향**: M2-4 HTTP 8000 fallback 정책은 `public_http_base`가 계속 존재한다는 전제에서 unsafe write 허용/차단 범위를 결정해야 한다. M2-9/M2-10에서 Supervisor/Front Router 구현 시 `WHATUDOIN_PUBLIC_BASE_URL` 주입이 필수다.

### M2-3 적용 범위 표

| 범위 | 현재 코드 위치 | 변경 후 형태 |
|------|----------------|--------------|
| `/settings/mcp` MCP endpoint 안내 URL | `app.py:739` | `WHATUDOIN_PUBLIC_BASE_URL` 또는 request-host fallback 기반 `https://<PC-IP>:8443/mcp/` 사용. 내부 loopback/`8000` 미노출 |
| MCP 클라이언트 가이드(Cline/Codex/Claude Desktop/Claude Code) | `app.py:746`, `app.py:758`, `app.py:770`, `app.py:779` | 모두 동일 `mcp_base` 사용 |
| 인증서 안내 HTTP→HTTPS probe | `app.py:475`, `templates/base.html:15` | `public_https_base`/`public_http_base` 사용. 기존 PC IP 기반 `8443`/`8000` 동작 유지 |
| AVR HTTPS→HTTP fallback | `app.py:4213` | `_public_base_url(request, 'http') + '/avr'` 사용 |
| `/remote` redirect | `app.py:4186` | 상대 redirect 유지. 외부 absolute URL 생성 필요 시 canonical helper 사용 |
| 체크리스트 viewer 링크 복사 | `templates/check.html:1809` | `public_http_base` 사용 |
| 체크리스트 editor 링크 복사 | `templates/check_editor.html:424` | `public_http_base` 사용 |
| 문서 editor 링크 복사 | `templates/doc_editor.html:618` | `public_http_base` 사용 |
| 외부 알림 링크 | 현재 해당 없음 | 향후 추가 시 `_public_base_url()`/`public_https_base`만 사용 |

---

## M2-4 완료 (2026-05-10) — HTTP 8000 fallback 인증/쓰기 범위 결정 + baseline 표

- **변경**: `app.py`에 `_HTTPFallbackWriteGuardMiddleware`를 추가해 HTTP scheme의 unsafe method(`POST`/`PUT`/`PATCH`/`DELETE`)를 공통 차단했다. 채택 정책은 **권장안: HTTP 8000 unsafe write 차단 + AVR whitelist 예외(`/avr`, `/remote`, `/api/avr`, `/api/avr/*`)**다. baseline/검증 자동화를 위해 `_workspace/perf/scripts/m2_4_http_fallback_probe.py`를 추가했다.
- **증거**: pre-policy baseline `_workspace/perf/baseline_2026-05-09/m2_4_004355_pre_policy/m2_4_http_fallback_probe.md` — IP whitelist 자동 로그인 + HTTP 8000 + 쿠키 없음 + matching Origin 조건에서 `/api/events` POST, `/api/doc/{id}` PUT, `/api/checklists` POST, `/api/upload/image` POST, `/api/upload/attachment` POST 모두 `200`으로 write 가능. post-policy `_workspace/perf/baseline_2026-05-09/m2_4_004435_post_policy/m2_4_http_fallback_probe.md` — 동일 조건에서 5개 unsafe write 모두 `403`, `/api/events` GET은 `200`. QA 요약: `.codex/workspaces/current/qa_report.md`.
- **회귀**: `python -m py_compile app.py database.py llm_parser.py auth.py crypto.py backup.py` 통과, `python -c "import app; print('OK')"` 통과, `git diff --check` 통과. post-policy probe cleanup 확인 — users 1, user_ips 1, meetings 1 정리, sessions 0, 업로드 파일 0, port 8000 종료 후 closed.
- **다음 step 영향**: M2-5 HSTS 결정은 HTTP 8000 fallback을 계속 유지하는 전제에서 판단해야 한다. 현재 정책상 HTTP 8000은 인증서 안내/redirect fallback/AVR 계열/읽기 허용에만 남고, 사용자 write는 HTTPS 8443 또는 향후 Front Router HTTPS 경유만 허용된다. M2-10/M2-14/M2-15에서 Front Router가 Web API로 내부 HTTP proxy를 쓰는 경우, 외부 HTTPS write가 앱에서 `scheme=https`로 재구성되도록 trusted proxy/forwarded proto 정책을 함께 고정해야 한다.

### M2-4 baseline/verification 표

| endpoint | pre-policy HTTP 8000 | post-policy HTTP 8000 | 판정 |
|---|---:|---:|---|
| `POST /api/events` | 200 | 403 | 정책 부합 |
| `PUT /api/doc/{id}` | 200 | 403 | 정책 부합 |
| `POST /api/checklists` | 200 | 403 | 정책 부합 |
| `POST /api/upload/image` | 200 | 403 | 정책 부합 |
| `POST /api/upload/attachment` | 200 | 403 | 정책 부합 |
| `GET /api/events` | 200 | 200 | non-write 유지 |

### M2-4 정책 재결정 (2026-05-10) — HTTP unsafe write 가드 제거 (사용자 의도 회복)

- **사유**: 사용자가 직접 의도를 명시 — 사내 인트라넷 망 운영 전제(§2)에서 HTTP/HTTPS 기능/권한이 동등해야 하고, HTTPS는 브라우저 알람(Notification API의 secure context 정책) 전용이다. M2-4 권장안(HTTP unsafe write 차단)은 인증서 미신뢰 PC 사용자가 로그인부터 못 하게 막아 의도와 정면 충돌. plan §13 (대안) "HTTP write 유지" 정책으로 재채택.
- **변경**: `app.py`에서 `_HTTPFallbackWriteGuardMiddleware` 클래스 + `_HTTP_FALLBACK_UNSAFE_METHODS` / `_HTTP_FALLBACK_WRITE_ALLOW_EXACT` / `_HTTP_FALLBACK_WRITE_ALLOW_PREFIXES` 상수 + `_is_http_fallback_write_allowed` predicate + `app.add_middleware(_HTTPFallbackWriteGuardMiddleware)` 호출 모두 제거. 정책 결정 사유를 코멘트로 기록(HTTP/HTTPS 기능 동등 + HTTPS 알람 전용 + plan §13 (대안) 채택).
- **회선 신뢰 보호 유지**: §2 사내 LAN 운영 전제 + Front Router strip-then-set forwarded headers(M2-11) + TRUSTED_PROXY 외부 직접 접근 차단(M2-13). 외부 위조 헤더는 그대로 차단됨.
- **회귀 테스트 갱신**: `tests/phase72_http_fallback_mcp_allowlist.py` 전면 재작성(14종 단언). [A] 운영 코드 grep 7종(가드 클래스/상수 4종/predicate 부재 + 정책 사유 코멘트), [B] ASGI level passthrough 7종(HTTP scheme + POST `/api/events`/`/mcp/`/`/avr`/`/api/login`, PUT `/api/doc/1`, DELETE `/api/events/1`, HTTPS scheme + POST `/api/events`) — 가드 전용 detail 문자열 "HTTP fallback에서는 쓰기 요청이 차단됩니다" 부재로 단언(인증/CSRF 정상 4xx와 가드 차단 분리).
- **명령**: `python -c "import app; print('OK')"` PASS, `D:\Program Files\Python\Python312\python.exe tests\phase72_http_fallback_mcp_allowlist.py` 14/14 PASS. phase54~73 20개 회귀 모두 PASS.
- **이전 fix 무효화**: 동일 사이클에 처리한 "M2-4 후속 fix MCP HTTP 화이트리스트"는 본 정책 재결정으로 자연 흡수. 가드 자체가 부재하므로 화이트리스트도 무관. phase72가 가드 부재 잠금으로 변경.
- **남은 운영 모델**: HTTP 8000 접속 사용자 → 모든 unsafe write 사용 가능, 알람만 동작 안 함(브라우저 secure context 정책상 자연 차단). HTTPS 8443 접속 사용자(인증서 신뢰 PC) → 동일 기능 + 알람 사용 가능. `_BrowserHTTPSRedirectMiddleware`(M2-15)가 인증서 신뢰 여부에 따라 자동 분기.
- **추가 fix (2026-05-10) — session cookie secure flag scheme 분기**: M2-4 가드 제거 후에도 HTTP에서 로그인이 안 되는 회귀 발견. `app.py:login`/`admin_login`/`logout` 응답의 `set_cookie`/`delete_cookie`가 `secure=True`로 박혀 있어서 HTTP 접속 시 브라우저가 session cookie를 저장하지 않음 → 로그인 200 응답이 와도 다음 요청에서 미인증. `secure=(request.url.scheme == "https")` 분기로 수정. HTTPS 접속자만 Secure flag 적용, HTTP 접속자는 cookie 정상 저장. 회귀 테스트 `tests/phase74_session_cookie_scheme_branching.py` 9/9 PASS — secure=True 하드코딩 0건 grep + 분기 패턴 ≥3건 grep + login/admin_login/logout 핸들러 영역별 분기 단언 + Starlette TestClient runtime 단언(HTTP 응답 Set-Cookie에 Secure 부재, HTTPS 응답에 Secure 존재). phase54~74 21개 회귀 PASS.

### M2-4 후속 fix (2026-05-10, 정책 재결정으로 자연 흡수) — MCP HTTP 화이트리스트 추가

- **사유**: 사용자 의도 — 인증서 미신뢰 PC 사용자도 MCP를 HTTP 8000으로 사용 가능해야 한다. M2-4 초기 정책은 AVR(`/avr`/`/remote`/`/api/avr/*`)만 화이트리스트 예외였고 MCP Streamable HTTP transport(POST 기반)가 차단되어 HTTP 사용자가 MCP를 못 쓰는 회귀가 발생했다.
- **변경**: `app.py:438~439` `_HTTP_FALLBACK_WRITE_ALLOW_EXACT`에 `"/mcp"` + `_HTTP_FALLBACK_WRITE_ALLOW_PREFIXES`에 `"/mcp/"` 추가. plan §13 (대안) "HTTP write 유지 + 명시 화이트리스트" 정책 일부 채택(MCP 한정).
- **회귀 테스트 추가**: `tests/phase72_http_fallback_mcp_allowlist.py`(12종 단언). HTTP+POST `/mcp/` / `/mcp/initialize` / `/mcp` 통과, HTTP+POST `/api/events` 차단 유지, HTTP+POST `/avr` 통과 회귀 보존, HTTPS+POST `/api/events` 분기 외 통과, HTTP+GET `/mcp/` unsafe method 아님 통과, predicate 직접 호출 5종.
- **명령**: `D:\Program Files\Python\Python312\python.exe tests\phase72_http_fallback_mcp_allowlist.py` PASS. phase54~71 회귀 모두 PASS.
- **별개 후속 (2026-05-10 같은 사이클에 처리)**: `/settings/mcp` 안내 URL이 외부 canonical(HTTPS) 기준이라 HTTP 사용자가 안내를 그대로 따르면 HTTPS로 가던 회귀를 fix. `app.py:settings_mcp_page` 핸들러에 `scheme = "http" if request.url.scheme == "http" else "https"` 분기 추가 후 `_public_base_url(request, scheme)` 호출. M2-3 canonical URL 정책의 핵심(host 통일)은 깨지 않음 — env `WHATUDOIN_PUBLIC_BASE_URL` 설정 시 host는 그 값으로 고정되고 scheme/port만 사용자 접속에 맞춰 분기. 회귀 테스트 `tests/phase73_settings_mcp_scheme_branching.py`(17종 단언, env 미설정/설정 양쪽 fallback + handler 분기 코드 grep + cline/codex/claude_desktop/claude_code 4종 안내가 같은 mcp_base 사용 단언) PASS. phase54~73 20개 회귀 PASS.

---

## M2-5 완료 (2026-05-10) — HSTS 적용 여부 결정

- **변경**: 운영 코드 변경 없음. 정책은 **HTTP fallback 유지 중 HSTS 미적용**으로 결정했다. M2-4에서 HTTP unsafe write는 차단했지만, HTTP 8000 자체는 인증서 안내/probe fallback, AVR 예외 흐름, non-write fallback 확인 경로로 남아 있으므로 `Strict-Transport-Security`는 아직 적용하지 않는다.
- **증거**: `rg -n -i 'strict-transport-security|hsts' app.py templates static\js` 결과 0건. 동적 probe `_workspace/perf/baseline_2026-05-09/m2_5_005255_hsts/m2_5_hsts_probe.md` — `https://127.0.0.1:8443/api/health` 200/HSTS absent, `https://127.0.0.1:8443/` 303/HSTS absent, `http://127.0.0.1:8000/api/health` 200/HSTS absent. QA 요약: `.codex/workspaces/current/qa_report.md`.
- **회귀**: `_workspace/perf/scripts/m2_5_hsts_probe.py` py_compile 통과. probe가 8000/8443 서버를 직접 시작/종료했고 종료 후 두 포트 모두 closed 확인. 운영 응답 헤더 변경 0.
- **다음 step 영향**: M2-6 AVR Front Router 호환 정책은 HTTP fallback이 계속 존재한다는 전제로 판단한다. HSTS 적용은 인증서 배포 완료, HTTP fallback 종료, AVR 의존성 정리 또는 HTTPS 전환 후에만 별도 step으로 재검토한다. 그때도 `max-age`는 7~30일처럼 짧게 시작하고 `includeSubDomains`/`preload`는 기본 미적용으로 둔다.

### M2-5 HSTS 정책 표

| 조건 | 결정 | max-age | includeSubDomains/preload | rollback 조건 |
|---|---|---:|---|---|
| HTTP fallback 유지 중 | HSTS 미적용 | 0 | 미적용 | 해당 없음 |
| HTTP fallback 종료 후 | 재검토 후 적용 가능 | 7~30일로 시작 | 기본 미적용 | 인증서 교체/오류 시 즉시 header 제거 |

---

## M2-6 완료 (2026-05-10) — AVR Front Router 호환 정책 결정

- **변경**: 운영 코드 변경 없음. 정책은 **(b) AVR 사용 배포**로 결정했다. 현재 코드가 AVR admin 설정, 사용자별 `avr_enabled`, `/remote` → `/avr` redirect, `/avr` IP whitelist 권한, 동적 `frame-src`, HTTP fallback 예외를 이미 갖고 있고, M2-4/M2-5도 AVR을 전제로 HTTP fallback을 유지했기 때문에 AVR을 M2 scope에서 제외하지 않는다.
- **증거**: 직접 동작 probe `_workspace/perf/baseline_2026-05-09/m2_6_005839_avr/m2_6_avr_policy_probe.md` — `/remote` 307 → `/avr`, session login `/avr` 403, IP whitelist + `avr_enabled` `/avr` 200, CSP `frame-src http://127.0.0.1:<fake-wudeskop-port>` 포함, HTTPS `/avr`에서 canonical HTTP fallback `http://127.0.0.1:8000/avr` 307 확인. QA 요약: `.codex/workspaces/current/qa_report.md`.
- **회귀**: `_workspace/perf/scripts/m2_6_avr_policy_probe.py` py_compile 통과. probe가 임시 AVR user/IP/session/settings와 fake WUDeskop token server를 사용했고 종료 시 users 1, user_ips 1, sessions 1 정리 및 AVR settings 복원, 8000/8443 closed 확인.
- **다음 step 영향**: M2-20 종료 검증에 AVR Front Router 회귀 5종을 반드시 포함한다. M2-7은 M2-2에서 SSE 접근 경로 (a)를 채택했기 때문에 별도 LAN 포트 (b) 옵션 비채택을 명시하면 된다.

### M2-6 AVR Front Router 회귀 시나리오 5종

| 번호 | 시나리오 | 통과 기준 |
|---:|---|---|
| 1 | 라우터 경유 `GET /remote` | `/avr`로 307 redirect되고, 외부 canonical URL/Host 정책과 충돌하지 않는다 |
| 2 | 라우터 경유 `GET /avr` 권한 흐름 | `login_via == "ip"` + `avr_enabled` 사용자만 AVR HTML을 받고, session login 또는 권한 없는 사용자는 기존 403 흐름을 유지한다 |
| 3 | CSP 보존 | `/avr` 응답 `Content-Security-Policy.frame-src`가 plain HTTP WUDeskop origin을 포함하고, Front Router가 CSP를 덮어쓰거나 strip하지 않는다 |
| 4 | HTTP fallback 예외 | M2-4 HTTP unsafe write 차단 정책이 `/avr`, `/remote`, `/api/avr`, `/api/avr/*` 예외와 충돌하지 않아 plain HTTP AVR iframe 흐름을 막지 않는다 |
| 5 | canonical iframe/redirect URL | AVR iframe의 WUDeskop viewer URL은 configured WUDeskop origin을 사용하고, HTTPS `/avr`에서 plain HTTP WUDeskop으로 내려갈 때 앱 fallback URL은 M2-3 canonical HTTP base를 사용한다 |

---

## M2-7 완료 (2026-05-10) — SSE 별도 LAN 포트 (b) 옵션 비채택 결정

- **변경**: 운영 코드 변경 없음. M2-2에서 사용자-facing SSE 접근 경로를 **(a) Front Router가 외부 `/api/stream`을 SSE service로 라우팅**으로 이미 채택했으므로, M2-7의 별도 LAN 포트 직접 공개 **(b) 옵션은 비채택**으로 닫는다.
- **증거**: `static/js/realtime.js:45`는 `new EventSource('/api/stream')`로 same-origin 상대 경로만 사용한다. `app.py:1979`는 기존 same-origin `/api/stream` route를 제공한다. `app.py:293` 기본 CSP는 `connect-src 'self'`이며, `rg -n 'new EventSource\(|withCredentials|Access-Control-Allow-Credentials|CORSMiddleware|https?://.*api/stream|connect-src .*https?://' app.py templates static\js` 결과는 `static/js/realtime.js:45` 단일 match뿐이다. QA 요약: `.codex/workspaces/current/qa_report.md`.
- **회귀**: 코드 변경 없음. `git diff --check` 통과. M2-7은 별도 LAN 포트 SSE 구현을 추가하지 않으므로 브라우저/CORS/cookie/cert 동작 변경 0.
- **다음 step 영향**: M2-10 Front Router 최소 구현은 외부 `/api/stream`을 유지하고 내부 SSE service로 proxy한다. 별도 SSE LAN origin을 만들지 않으므로 `connect-src` 추가 origin, `EventSource.withCredentials`, CORS `Allow-Credentials`, cookie Domain/SameSite 변경, HTTPS SAN 추가는 M2 범위에서 구현하지 않는다.

### M2-7 (b) 옵션 비용 처리 표

| 비용 항목 | 결정 | 근거 |
|---|---|---|
| CSP `connect-src` 외부 SSE origin 추가 | 미구현 | same-origin `/api/stream` 유지 |
| `EventSource` credential 옵션 | 미구현 | same-origin cookie 흐름 유지 |
| CORS `Access-Control-Allow-Credentials` | 미구현 | cross-origin SSE 미사용 |
| cookie Domain/SameSite 조정 | 미구현 | 별도 origin/port 직접 접근 미사용 |
| HTTPS SAN/certificate 확장 | 미구현 | 사용자-facing origin은 기존 Front Router origin 유지 |

---

## M2-8 완료 (2026-05-10) — PyInstaller frozen self re-spawn 검증

- **변경**: 운영 코드 변경 없음. `_workspace/perf/scripts/m2_8_pyinstaller_respawn_probe.py`를 추가해 최소 supervisor/service entry를 onedir로 빌드하고, frozen exe가 자기 자신을 `sys.executable --service=echo` 형태로 재실행하는 패턴을 검증했다.
- **증거**: PyInstaller 6.19.0. 검증 결과 `_workspace/perf/pyinstaller_m2_8/runs/20260510_010409/m2_8_pyinstaller_respawn_probe.md` — onedir exe `_workspace/perf/pyinstaller_m2_8/dist/M2RespawnProbe/M2RespawnProbe.exe` 빌드 성공, parent/child `sys.frozen=True`, child argv `--service=echo`, child executable이 parent `sys.executable`과 동일, `multiprocessing.freeze_support()` 호출, stop flag 기반 graceful exit 0. QA 요약: `.codex/workspaces/current/qa_report.md`.
- **회귀**: `_workspace/perf/scripts/m2_8_pyinstaller_respawn_probe.py` py_compile 통과. WhatUdoin 앱 서버/DB 변경 없음. 실제 build/run 산출물은 `_workspace/perf/pyinstaller_m2_8/` 하위에 격리했다.
- **다음 step 영향**: M2-9 Supervisor 골격은 이 검증된 primitive를 전제로 설계한다. 단, 이번 검증은 최소 probe이며 전체 WhatUdoin 패키징 검증은 아니다. 실제 Web API/SSE service packaging, service별 로그/health/restart_count, crash-loop, graceful shutdown은 M2-9/M2-17/M2-20에서 별도 증거가 필요하다.

### M2-8 frozen respawn 검증 표

| 항목 | 결과 |
|---|---|
| onedir build | PASS |
| `sys.executable --service=<name>` spawn | PASS |
| parent `sys.frozen=True` | PASS |
| child `sys.frozen=True` | PASS |
| child service argv 인식 | PASS |
| `multiprocessing.freeze_support()` 호출 | PASS |
| graceful shutdown | PASS |

---

## M2-9 완료 (2026-05-10) — Supervisor 골격 도입

- **변경**: `supervisor.py`를 추가했다. 7단계 startup 순서(`M2_STARTUP_SEQUENCE`), `_RUN_DIR/internal_token` 생성 + ACL 적용 시도, 내부 토큰/토큰 파일/service name env 전달, 서비스별 pid/status/restart_count/last_error/log path 스냅샷, startup 실패와 runtime crash 카운터 분리를 구현했다.
- **증거**: `_workspace/perf/scripts/m2_9_supervisor_probe.py` / `_workspace/perf/supervisor_m2_9/runs/20260510_011022/m2_9_supervisor_probe.md`. 검증 결과: startup sequence 7단계 PASS, internal_token 파일 존재/비어있지 않음/ACL applied PASS, child env token=file 일치 PASS, service name env PASS, service pid PASS, startup failure counter only PASS, runtime crash counter only PASS, service log path 존재 PASS. QA 요약: `.codex/workspaces/current/qa_report.md`.
- **회귀**: `python -m py_compile supervisor.py _workspace/perf/scripts/m2_9_supervisor_probe.py` 통과. 실제 WhatUdoin 앱 서버를 띄우지 않는 isolated child-process probe로 검증했으며 운영 DB/라우트 변경 없음.
- **다음 step 영향**: M2-10 Front Router 최소 구현은 `supervisor.py`의 startup 순서와 내부 토큰/env 전달을 기반으로 외부 8000/8443 listener 및 route dispatch를 붙인다. M2-17에서 healthcheck, 자동 재시작, crash-loop degraded, 내부 publish 토큰 인증을 이 골격 위에 보강해야 한다.

### M2-9 Supervisor startup 순서

| 순서 | 단계 |
|---:|---|
| 1 | `resolve_runtime_paths` |
| 2 | `ensure_internal_token_file` |
| 3 | `prepare_shared_service_environment` |
| 4 | `start_front_router_listener` |
| 5 | `start_web_api_service` |
| 6 | `start_sse_service` |
| 7 | `verify_health_and_publish_status` |

### M2-9 카운터 분리 검증

| 케이스 | status | startup_failures | runtime_crashes | last_error |
|---|---|---:|---:|---|
| startup immediate exit | `failed_startup` | 1 | 0 | `startup exited with code 7` |
| runtime crash after startup grace | `crashed` | 0 | 1 | `runtime exited with code 9` |

---

## M2-10 완료 (2026-05-10) — Front Router 최소 구현

- **변경**: `front_router.py`를 추가했다. `FrontRouter` ASGI dispatcher가 `/api/stream`은 logical SSE app으로, `/`·`/api/*`·그 외 public path는 Web API app으로, `/uploads/meetings/*`는 Web API app으로 보낸다. `/internal/*`은 downstream app에 도달하기 전에 404로 차단한다.
- **증거**: `_workspace/perf/scripts/m2_10_front_router_probe.py` / `_workspace/perf/front_router_m2_10/runs/20260510_012803/m2_10_front_router_probe.md`. 검증 결과: `/api/stream`→SSE PASS, `/api/events`→Web API PASS, `/`→Web API PASS, `/uploads/meetings/2026/05/test.png`→Web API PASS, `/internal/publish`→404 + downstream calls 0 PASS.
- **회귀**: `python -m py_compile front_router.py _workspace/perf/scripts/m2_10_front_router_probe.py` 통과. 기본 `main.py`/tray 실행 경로는 아직 바꾸지 않아 현재 운영 앱 startup은 미변경이다.
- **범위 제한**: M2-10은 route ownership skeleton만 적용했다. 실제 SSE broker 이전과 `/internal/publish`는 M2-16/M2-17, SSE reverse proxy의 buffering/compression/idle timeout/disconnect 전파 조건은 M2-19에서 처리한다. `expose_route_headers`는 검증용 opt-in이며 기본값은 비활성이라 운영 응답에 router target을 노출하지 않는다.
- **다음 step 영향**: M2-11은 이 라우터 경계에서 외부 inbound forwarded 헤더를 폐기하고 라우터가 본 값으로 재작성하는 정책을 붙여야 한다. M2-13의 `TRUSTED_PROXY`/외부 직접 접근 차단, M2-14의 CSRF Host 보존, M2-15의 HTTPS probe middleware 호환은 같은 신뢰 경계를 전제로 진행한다.

### M2-10 route dispatch 검증

| 케이스 | 경로 | status | router target | downstream | 결과 |
|---|---|---:|---|---|---|
| SSE stream | `/api/stream` | 200 | `sse_service` | `sse` | PASS |
| 일반 API | `/api/events` | 200 | `web_api_service` | `web_api` | PASS |
| 화면 route | `/` | 200 | `web_api_service` | `web_api` | PASS |
| 보호 업로드 | `/uploads/meetings/2026/05/test.png` | 200 | `web_api_service` | `web_api` | PASS |
| 내부 endpoint 외부 차단 | `/internal/publish` | 404 | `blocked` | 없음 | PASS |

---

## M2-11 완료 (2026-05-10) — strip-then-set forwarded 헤더 정책

- **변경**: `front_router.py`에 `strip_then_set_forwarded_headers(scope)`를 추가했다. downstream dispatch 전 inbound `Forwarded`, `X-Forwarded-For`, `X-Forwarded-Host`, `X-Forwarded-Proto`, `X-Forwarded-Port`, `X-Real-IP`를 제거하고 라우터가 본 `scope["client"][0]`, `Host`, `scope["scheme"]`, port 값으로 `X-Forwarded-For`, `X-Forwarded-Host`, `X-Forwarded-Proto`, `X-Forwarded-Port`, `X-Real-IP`를 새로 세팅한다.
- **증거**: `_workspace/perf/scripts/m2_11_forwarded_probe.py` / `_workspace/perf/front_router_m2_11/runs/20260510_013222/m2_11_forwarded_probe.md`. 검증 결과: Web API path와 SSE path 모두 위조 `X-Forwarded-For: 10.10.10.10, 198.51.100.9`, `X-Forwarded-Host: attacker.example`, `X-Forwarded-Proto: http`, `X-Forwarded-Port: 8000`, `X-Real-IP: 198.51.100.9`, `Forwarded: for=10.0.0.5...` 원문이 downstream에 남지 않았다.
- **보존 확인**: `Host: whatudoin.local:8443`, `Cookie: session_id=test-session`, `Authorization: Bearer keep-me`는 downstream에 유지됐다. `/internal/publish`는 404로 차단되고 downstream calls 0건이었다.
- **회귀**: `python -m py_compile front_router.py _workspace/perf/scripts/m2_11_forwarded_probe.py` 통과. 기본 `main.py`/tray 실행 경로는 아직 미변경이다.
- **범위 제한**: M2-11은 라우터가 신뢰 가능한 forwarded 값을 만드는 단계다. `auth.get_client_ip()`, SlowAPI limiter, CSRF Host 처리, 외부 직접 internal port 접근 차단은 아직 바꾸지 않았고 M2-12~M2-14에서 각각 처리한다.
- **다음 step 영향**: M2-12는 limiter key를 `auth.get_client_ip()` 또는 동등한 trusted-proxy 판정으로 통일해야 한다. M2-13에서 `TRUSTED_PROXY`와 외부 직접 접근 차단을 한 세트로 검증해야 M2-11의 헤더 재작성 정책이 실제 인증/IP whitelist 경계와 연결된다.

### M2-11 forwarded 헤더 검증

| 케이스 | 경로 | downstream | 결과 |
|---|---|---|---|
| Web API forwarded 재작성 | `/api/events` | `web_api` | PASS |
| SSE forwarded 재작성 | `/api/stream` | `sse` | PASS |
| 내부 endpoint downstream 차단 | `/internal/publish` | 없음 | PASS |

---

## M2-12 완료 (2026-05-10) — SlowAPI limiter trusted-proxy 통일

- **변경**: `app.py`의 SlowAPI limiter를 `Limiter(key_func=auth.get_client_ip)`로 변경하고 `slowapi.util.get_remote_address` import를 제거했다. 인증/IP whitelist와 rate limit이 같은 trusted-proxy 판정 함수를 사용한다.
- **회귀 테스트 추가**: `tests/phase54_limiter_trusted_proxy.py`를 추가했다.
- **증거**: `_workspace/perf/scripts/m2_12_limiter_probe.py` / `_workspace/perf/limiter_m2_12/runs/20260510_014210/m2_12_limiter_probe.md`. 검증 결과: limiter key function `auth.get_client_ip` PASS, trusted proxy `127.0.0.1` 경유 50개 forwarded IP distinct bucket PASS, trusted proxy의 첫 forwarded IP 채택 PASS, 비신뢰 source의 `X-Forwarded-For` 위조 무시 PASS, forwarded header 없는 trusted proxy 요청 peer fallback PASS, 동일 IP `/api/login` 11번째 429 PASS.
- **명령**: `python -m py_compile app.py tests/phase54_limiter_trusted_proxy.py _workspace/perf/scripts/m2_12_limiter_probe.py` PASS, `python tests/phase54_limiter_trusted_proxy.py` PASS, `python _workspace/perf/scripts/m2_12_limiter_probe.py` PASS.
- **범위 제한**: M2-12는 limiter key 통일만 적용했다. `TRUSTED_PROXY` 실제 운영 값, Web API 내부 listener 외부 직접 접근 차단, 외부 직접 포트 호출 실패 검증은 M2-13 범위다.
- **다음 step 영향**: M2-13에서 `TRUSTED_PROXY` 설정과 내부 포트 loopback/방화벽 차단을 같이 검증해야 한다. `TRUSTED_PROXY`만 설정하고 외부 직접 접근 차단이 없으면 M2-12의 trusted proxy 통일이 IP whitelist 위조 리스크로 이어질 수 있다.

### M2-12 limiter 검증

| 케이스 | 결과 |
|---|---|
| limiter key function = `auth.get_client_ip` | PASS |
| trusted proxy 경유 50개 forwarded IP distinct bucket | PASS |
| trusted proxy 첫 forwarded IP 채택 | PASS |
| 비신뢰 source의 XFF 위조 무시 | PASS |
| 동일 IP 11번째 `/api/login` 429 | PASS |

---

## M2-13 완료 (2026-05-10) — TRUSTED_PROXY + 외부 직접 접근 차단 한 세트

- **변경**: `app.py`에 `WHATUDOIN_WEB_API_INTERNAL_ONLY` 기반 `_FrontRouterAccessGuardMiddleware`를 추가해 internal mode에서 non-loopback 직접 접근을 403으로 차단하고 loopback Front Router peer만 downstream으로 통과시킨다. `main.py`와 direct `python app.py` launcher는 `WHATUDOIN_BIND_HOST`를 지원하되 기본값은 기존 `0.0.0.0`으로 유지한다. `supervisor.py`에는 `web_api_internal_service_env()`와 `web_api_service_spec()`을 추가해 `TRUSTED_PROXY=127.0.0.1`, `WHATUDOIN_BIND_HOST=127.0.0.1`, `WHATUDOIN_WEB_API_INTERNAL_ONLY=1`을 한 세트로 적용하고, `extra_env`가 이 3개 보호 키를 덮어써 신뢰 경계를 분리하지 못하게 했다.
- **회귀 테스트 추가**: `tests/phase55_front_router_trust_boundary.py`.
- **증거**: `_workspace/perf/scripts/m2_13_trusted_proxy_probe.py` / `_workspace/perf/trusted_proxy_m2_13/runs/20260510_020446/m2_13_trusted_proxy_probe.md`. 검증 결과: command 3/3 PASS, focused boundary assertion 16/16 PASS, generated probe matrix 2/2 PASS. code-reviewer 재검토 APPROVED: `.codex/workspaces/current/code_review_report.md`. QA 재검증 PASS: `.codex/workspaces/current/qa_report.md`.
- **명령**: `D:\Program Files\Python\Python312\python.exe -m py_compile app.py main.py supervisor.py auth.py tests\phase55_front_router_trust_boundary.py _workspace\perf\scripts\m2_13_trusted_proxy_probe.py` PASS, `D:\Program Files\Python\Python312\python.exe tests\phase55_front_router_trust_boundary.py` PASS, `D:\Program Files\Python\Python312\python.exe _workspace\perf\scripts\m2_13_trusted_proxy_probe.py` PASS, `git diff --check -- app.py main.py supervisor.py tests\phase55_front_router_trust_boundary.py` PASS.
- **검증 항목**: supervisor factory가 Web API service name/command를 보존하고 internal env package를 함께 적용, `extra_env` 보호 키 override 차단, `main.py`/direct `app.py` bind host env 지원, trusted loopback proxy의 첫 `X-Forwarded-For` 채택, 비신뢰 direct source의 `X-Forwarded-For` 위조 무시, internal-only guard의 non-loopback 403 + loopback 허용, env 미설정 기본 runtime 호환, missing `scope["type"]`/lifespan passthrough.
- **범위 제한**: 이번 검증은 isolated Python/ASGI/source probe다. 실제 외부 LAN 장비에서 live Web API listener 직접 호출이 실패하는지는 물리 네트워크로 증명하지 않았다. 다만 supervisor/main/app launcher가 loopback bind를 적용하고, ASGI `scope.client` 기준 non-loopback 직접 접근은 403으로 검증했다. M2-20 정책별 증거 인덱스에서 실제 배포/네트워크 증거가 필요하면 별도 live probe로 보강한다.
- **다음 step 영향**: M2-14는 이 신뢰 경계를 전제로 Front Router가 unsafe request의 Host/Origin 정합성을 깨지 않도록 Host 보존 또는 신뢰된 `X-Forwarded-Host` 정책을 적용해야 한다. M2-15 HTTPS probe middleware router 호환은 아직 미구현 상태로 남긴다.

### M2-13 trust boundary 검증

| 케이스 | 결과 |
|---|---|
| supervisor Web API env package: `TRUSTED_PROXY` + loopback bind + internal-only guard | PASS |
| `extra_env`가 보호 env 3종을 덮어쓰지 못함 | PASS |
| `main.py`와 direct `app.py` launcher의 `WHATUDOIN_BIND_HOST` 지원 | PASS |
| trusted loopback proxy의 첫 `X-Forwarded-For` 채택 | PASS |
| 비신뢰 source의 `X-Forwarded-For` 위조 무시 | PASS |
| internal-only mode non-loopback 직접 접근 403 + downstream 0건 | PASS |
| internal-only mode loopback router peer 허용 | PASS |
| env 미설정 기본 runtime 호환 | PASS |

---

## M2-14 완료 (2026-05-10) — Front Router CSRF Host 보존 조건

- **결정**: 마스터 plan §13 두 옵션 중 **Host 보존(권장) 채택**. 운영 코드 변경 0건. 근거 — (a) `front_router.py` `FORWARDED_HEADER_NAMES`에 `host`가 없어 inbound `Host`는 strip되지 않고 downstream에 그대로 전달되며, (b) `FrontRouter.__call__`은 ASGI scope를 별도 proxy hop 없이 sub-app으로 forward하므로 Host 헤더가 외부 원본 그대로 보존되고, (c) `app.py:_check_csrf()`(line 640~647) 정책 "Host == Origin/Referer netloc"이 코드 변경 없이 통과한다. (d) M2-11 strip-then-set이 inbound `X-Forwarded-Host` 위조를 라우터 값으로 덮어쓰며 `_check_csrf()`는 Host만 봐서 X-Forwarded-Host 위조 영향이 없다.
- **외부 도메인 다중 정책**: PC LAN IP(`192.168.0.18:8443`)와 사내 hostname을 둘 다 사용하는 환경에서, 각 origin 내부 요청은 모두 통과하고 cross-origin POST는 403으로 차단된다. `_check_csrf()`는 Host와 Origin/Referer netloc 정확 일치만 보므로 origin 별 분기 코드가 필요 없다.
- **회귀 테스트 추가**: `tests/phase56_front_router_csrf_host.py`.
- **증거**: `_workspace/perf/scripts/m2_14_csrf_host_probe.py` / `_workspace/perf/csrf_host_m2_14/runs/20260509T172318Z/m2_14_csrf_probe.md`. 검증 결과: probe 15/15 PASS, phase56 회귀 15/15 PASS.
- **명령**: `D:\Program Files\Python\Python312\python.exe tests\phase56_front_router_csrf_host.py` PASS, `D:\Program Files\Python\Python312\python.exe _workspace\perf\scripts\m2_14_csrf_host_probe.py` PASS. `git diff app.py front_router.py auth.py supervisor.py` 출력 없음(운영 코드 변경 0건).
- **검증 항목**: `host` 헤더가 strip 대상에서 제외, 일반 POST downstream Host 보존 + X-Forwarded-Host = 외부 원본, 위조 `X-Forwarded-Host: attacker.example` 시 downstream Host 불변 + X-Forwarded-Host 라우터 재작성, SSE path 동일 보존, `_check_csrf` 4종(LAN IP same-origin/hostname same-origin/cross-origin 403/src 없음 우회), 다중 도메인 (LAN IP + hostname) 동일 origin 통과 + 교차 403.
- **범위 제한**: isolated ASGI level 검증이다. 실제 브라우저 unsafe POST/PUT/DELETE를 라우터 경유로 보내는 라이브 시나리오는 M2-20 정책별 증거 인덱스에서 추가 확인 가능. _check_csrf는 src(Origin/Referer)가 비어있으면 검증을 우회하므로(현재 정책) 그 점은 후속 보안 강화 후보로 §18에 남길 만하지만 M2-14 범위는 아니다.
- **다음 step 영향**: M2-15 HTTPS probe middleware는 Host 헤더를 신뢰할 수 있음을 전제로 한다. M2-14에서 라우터가 Host를 scope mutation 없이 보존함을 확인했으므로, M2-15는 Host 헤더를 권위적 source로 사용해 라우터 경유/외부 직접 접속을 분기해도 된다.

### M2-14 CSRF Host 보존 검증

| 케이스 | 결과 |
|---|---|
| `host` 헤더 strip 대상 제외 (`FORWARDED_HEADER_NAMES`) | PASS |
| 일반 POST downstream Host 보존 + X-Forwarded-Host = 외부 원본 | PASS |
| 위조 X-Forwarded-Host 라우터 재작성(downstream Host 불변) | PASS |
| SSE path Host 보존 + X-Forwarded-Host 외부 원본 | PASS |
| `_check_csrf` LAN IP same-origin 통과 | PASS |
| `_check_csrf` hostname same-origin 통과 | PASS |
| `_check_csrf` cross-origin 403 | PASS |
| `_check_csrf` src 없음 우회(현재 정책) | PASS |
| 다중 도메인 LAN IP + hostname 각 origin 통과 + 교차 403 | PASS |

---

## M2-15 완료 (2026-05-10) — HTTPS probe middleware 라우터 호환

- **결정**: 마스터 plan §13 두 옵션 중 **scheme 보존(=재구성과 동등) 채택**. 운영 코드 변경 0건. 근거 — `front_router.py`의 `FrontRouter.__call__`은 ASGI scope를 `await app(scope, ...)`로 sub-app에 직접 dispatch하고 `_scope_with_router_forwarded_headers`는 `{**scope, "headers": ...}` shallow copy로 `scope["scheme"]` 키를 건드리지 않는다. 외부 https(8443) connection의 `scope["scheme"]=https`가 그대로 downstream Web API에 전달돼 `_BrowserHTTPSRedirectMiddleware`의 `scope.get("scheme") != "http"` 분기로 자연 통과한다. 외부 직접 http(8000) connection은 `scope["scheme"]=http` → middleware 동작 → probe HTML 응답. plan §13 "라우터 → Web API 구간은 항상 scheme=http"는 일반 reverse proxy 가정으로, ASGI dispatcher 구조에서는 자연 해소.
- **회귀 테스트 추가**: `tests/phase57_https_probe_router_compat.py`.
- **증거**: `_workspace/perf/scripts/m2_15_https_probe_compat.py` / `_workspace/perf/https_probe_m2_15/runs/20260509T173402Z/m2_15_https_probe_compat.md`. 검증 결과: probe 23/23 PASS, phase57 회귀 23/23 PASS.
- **명령**: `D:\Program Files\Python\Python312\python.exe tests\phase57_https_probe_router_compat.py` PASS, `D:\Program Files\Python\Python312\python.exe _workspace\perf\scripts\m2_15_https_probe_compat.py` PASS. `git diff app.py front_router.py auth.py supervisor.py main.py` 출력 없음(운영 코드 변경 0건).
- **검증 항목**: forwarded headers 빌드가 scope.scheme을 변경하지 않음(https/http 양쪽), https inbound downstream 도달 + probe HTML 시그니처 0건 + downstream scheme 보존, http 브라우저 navigate inbound probe HTML(title `알람용 인증서 확인 중`/cookie 셋팅/`fetch(httpsBase + '/api/health'`/200/downstream 미도달), `wd-cert-skip=1` cookie + http 우회, `/api*` skip prefix 우회, HEAD 우회, CORS non-navigate 우회, 라우터 미경유 직접 http 8000 접속에서도 동일 middleware 동작 유지.
- **범위 제한**: isolated ASGI level 검증이다. SSE path(`/api/stream`)는 `_SKIP_PREFIXES`에 `/api`로 매칭돼 middleware 첫 분기에서 즉시 통과한다. 라이브 브라우저에서 라우터 경유 외부 8000 fallback 시 probe HTML 동작은 M2-20 정책별 증거 인덱스에서 라이브 회귀로 확인 가능. M2-13 internal-only mode와 함께 운영하면 외부 8000 직접 접속도 Front Router를 통과하므로 라우터 경유 시나리오와 동일 결과를 받는다.
- **다음 step 영향**: M2-16 SSE broker 프로세스 이전은 scheme 보존이 SSE path에서도 동일하게 동작한다. SSE path는 `_BrowserHTTPSRedirectMiddleware._SKIP_PREFIXES`(`/api`)에 의해 별도 분기 없이 통과하므로 SSE service 분리 시 별도 scheme 처리 코드가 필요 없다.

### M2-15 HTTPS probe 호환 검증

| 케이스 | 결과 |
|---|---|
| forwarded scope build가 scope.scheme 변경 안 함 (https/http 양쪽) | PASS |
| https inbound → downstream 도달 + probe HTML 0건 + scheme 보존 | PASS |
| http 브라우저 navigate inbound → probe HTML 응답(title/cookie/fetch/200) | PASS |
| `wd-cert-skip=1` cookie + http → middleware 우회 | PASS |
| `/api*` skip prefix + http → middleware 우회 | PASS |
| HEAD 요청 → middleware 우회 | PASS |
| CORS non-navigate Sec-Fetch-Mode → middleware 우회 | PASS |
| 라우터 미경유 직접 http 8000 → probe HTML 응답 | PASS |
| 운영 코드 변경 0건 | PASS |

---

## M2-16 완료 (2026-05-10) — SSE broker SSE service 프로세스로 이전

- **변경**: 신규 파일 `publisher.py`(publish 추상화 + IPC HTTP POST + sse_publish_failure 카운터), `sse_service.py`(Starlette ASGI app: `GET /api/stream` + `POST /internal/publish` loopback guard + 자체 `SSEBroker` 인스턴스 + lifespan). `app.py`의 `wu_broker.publish(...)` 37개 호출을 `from publisher import publish as _sse_publish` 단일 추상화 함수로 일괄 교체. `app.py`의 `/api/stream` 핸들러는 `WHATUDOIN_SSE_SERVICE_URL` env 설정 시 503 응답으로 분리 모드 알림. `supervisor.py`에 `SSE_SERVICE_NAME="sse"`, `SSE_SERVICE_BIND_HOST_ENV`, `SSE_SERVICE_PORT_ENV`, `SSE_SERVICE_PUBLISH_URL_ENV`, `SSE_SERVICE_URL_ENV`, `SSE_SERVICE_DEFAULT_PORT=8765`, `sse_service_spec()` factory(loopback bind 강제 + 포트 보호 env override 차단) 추가.
- **publish 환경 분기**: `WHATUDOIN_SSE_PUBLISH_URL` 설정 시 stdlib `urllib.request.urlopen`으로 IPC POST(timeout 1.0s, silent fail + sse_publish_failure 카운터 증가, `WHATUDOIN_INTERNAL_TOKEN` 있으면 `Authorization: Bearer` 첨부 — 토큰 강제는 M2-17 범위). 미설정 시 in-process `wu_broker.publish` fallback. 외부 의존성 추가 0건(stdlib만 사용).
- **회귀 테스트 추가**: `tests/phase58_sse_broker_relocation.py`(19종 단언). `tests/phase57_https_probe_router_compat.py`의 M2-15 시점 한정 단언 `I_no_operational_code_changes`(git diff 기반)를 제거(후속 M2 step에서 운영 코드 수정이 정상이라 영구 FAIL이 됨, M2-15 시점 PASS 증거는 진행 결과 문서에 보존).
- **증거**: `_workspace/perf/scripts/m2_16_sse_broker_relocation_probe.py` / `_workspace/perf/sse_broker_m2_16/runs/20260509T180041Z/m2_16_sse_broker_probe.md`. probe 19/19 PASS, phase58 19/19 PASS.
- **명령**: `D:\Program Files\Python\Python312\python.exe tests\phase58_sse_broker_relocation.py` PASS, `D:\Program Files\Python\Python312\python.exe _workspace\perf\scripts\m2_16_sse_broker_relocation_probe.py` PASS. phase54/55/56/57 회귀 모두 PASS(I assertion 제거 후 phase57 22종 단언 PASS).
- **검증 항목**: in-process fallback 큐 메시지 수신 / IPC mock HTTP 서버 publish 도착(event/data) / IPC unreachable silent fail + 카운터=1 / IPC `Authorization: Bearer` 헤더 첨부 / SSE service `/api/stream` 200 + `text/event-stream` + Cache-Control no-cache + X-Accel-Buffering no + ': connected' 수신 / SSE service `/internal/publish` loopback 200 ok / 잘못된 JSON 400 / data 누락 400 / 외부 IP 403 / app.py `/api/stream` SSE_SERVICE_URL 설정 시 503 / 미설정 시 정상 fallback 200 + text/event-stream + connected.
- **범위 제한**: 본 step은 broker 이전·publish 추상화·loopback IP 가드까지. **내부 토큰 강제 검증·healthcheck·로그 분리·crash-loop 차단은 M2-17 범위.** publish 실패 카운터 expose/metric은 M2-18 범위. SSE proxy 6종 조건(buffering/compression/idle/disconnect/header/internal block)은 M2-19 범위. supervisor가 sse_service_spec을 사용해 실제 spawn하는 운영 통합은 M2-17 / M2-20.
- **다음 step 영향**: M2-17은 publisher.py에 마련된 `WHATUDOIN_INTERNAL_TOKEN` 자리에 검증 강제 + sse_service.py `/internal/publish`에 토큰 검증 + `/healthz` 추가 + supervisor crash-loop 카운터 활용. M2-18은 publisher.sse_publish_failure를 metric/log 채널로 expose. M2-19는 Front Router의 SSE service proxy 동작 6종을 sse_app 인자로 채운 상태에서 검증.

### M2-16 SSE broker 이전 검증

| 케이스 | 결과 |
|---|---|
| publisher.publish in-process fallback (wu_broker subscribe 큐 수신) | PASS |
| publisher.publish IPC POST mock 서버 도착 (event/data) | PASS |
| publisher.publish IPC unreachable silent + sse_publish_failure 1 증가 | PASS |
| publisher.publish IPC Authorization Bearer 첨부 | PASS |
| sse_service `/api/stream` 200 + SSE 헤더 4종 + connected 수신 | PASS |
| sse_service `/internal/publish` loopback 200 ok | PASS |
| sse_service `/internal/publish` 잘못된 JSON 400 / data 누락 400 | PASS |
| sse_service `/internal/publish` 외부 IP 403 | PASS |
| app.py `/api/stream` SSE_SERVICE_URL 설정 시 503 | PASS |
| app.py `/api/stream` 미설정 시 정상 fallback 200 + connected | PASS |
| phase54/55/56/57 회귀 모두 PASS (phase57 I assertion 제거 후 22종) | PASS |

---

## M2-17 완료 (2026-05-10) — 내부 토큰 인증 + healthcheck + crash-loop 차단

- **변경**: `sse_service.py`에 `_verify_internal_token()` 추가(loopback IP guard → Authorization Bearer 추출 → `secrets.compare_digest()` timing-safe 비교, env 미설정 시 모든 IPC 거부, 실패 로그에 raw token 0건 — 메타 문자열만), `/internal/publish` 핸들러가 토큰 미일치 시 401 응답. `GET /healthz` 라우트 신설(`{"status":"ok","subscribers":<int>,"service":"sse"}`). `app.py`에 `/healthz` 라우트 추가(`{"status":"ok","service":"web-api"}`) + `_BrowserHTTPSRedirectMiddleware._SKIP_EXACT`에 `/healthz` 등록(probe 미개입). `supervisor.py`에 `CRASH_LOOP_WINDOW_SECONDS=300`/`CRASH_LOOP_MAX_FAILURES=3` 상수, `ServiceState.crash_history: list[float]` 필드, `_record_crash`/`_is_crash_loop`/`reset_crash_loop()` 추가, `start_service`가 crash-loop 진입 시 spawn 시도 없이 status=`degraded` + last_error="crash-loop blocked", `poll_service`도 crash 기록·degraded 격상 처리. supervisor protected env에 `INTERNAL_TOKEN_ENV` 추가(`web_api_service_spec`/`sse_service_spec` 모두). `probe_healthz()` 헬퍼(stdlib `urllib.request`로 GET /healthz 1.0s timeout, ok 응답 확인) 정의 — 실제 watchdog 폴링 루프는 M2-20 통합 범위.
- **회귀 테스트 추가**: `tests/phase59_sse_token_health_crashloop.py`(22종 단언). `tests/phase58_sse_broker_relocation.py`의 [6] 시나리오를 토큰 헤더 포함으로 업데이트(IPC 토큰 의무화 후에도 401/200 분기 검증 유지).
- **증거**: `_workspace/perf/scripts/m2_17_token_health_crashloop_probe.py` / `_workspace/perf/m2_17_health_token_crashloop/runs/20260509T181437Z/probe.md`. probe 22/22 PASS, phase59 22/22 PASS.
- **명령**: `D:\Program Files\Python\Python312\python.exe tests\phase59_sse_token_health_crashloop.py` PASS, `D:\Program Files\Python\Python312\python.exe _workspace\perf\scripts\m2_17_token_health_crashloop_probe.py` PASS. phase54(5/5)/55(16/16)/56(15/15)/57(22/22)/58(19/19) 회귀 모두 PASS.
- **검증 항목**: SSE service `/internal/publish` env 미설정 + Authorization 없음 → 401, env 미설정 + Authorization 있음 → 401, env 설정 + Authorization 누락 → 401, env 설정 + 잘못된 토큰 → 401, env 설정 + 정확한 토큰 → 200, `secrets.compare_digest` 사용 grep PASS, 로그에 토큰 raw 값 0건 / SSE service `/healthz` 200 + JSON 시그니처 + subscribers 카운트 / Web API `/healthz` 200 + JSON 시그니처 + `_SKIP_EXACT` 매칭 / supervisor crash-loop: 5분 안 3회 누적 → status=`degraded`, 4번째 start_service 호출도 spawn 시도 없이 degraded 유지, reset_crash_loop 후 정상 spawn / `probe_healthz` 200+ok=True, timeout=False, 404=False.
- **범위 제한**: 실제 watchdog 주기 폴링/트레이 UI 표시는 M2-20 통합. publish 실패 카운터 metric expose는 M2-18. supervisor live spawn (`subprocess.Popen`)으로 SSE service 띄워 토큰 검증·healthcheck를 라이브 회귀로 확인하는 시나리오는 M2-20.
- **다음 step 영향**: M2-18은 SSE service `/healthz`에 `failures` 키 추가 + publisher.sse_publish_failure를 metric/access log에 expose. M2-19는 Front Router의 SSE proxy 6종 조건이 토큰/healthcheck 인증 흐름과 호환되는지 검증. M2-20은 supervisor가 실제 SSE service를 spawn해 토큰 + healthcheck + crash-loop를 라이브 통합 검증.

### M2-17 토큰/healthcheck/crash-loop 검증

| 케이스 | 결과 |
|---|---|
| `/internal/publish` env 미설정 + Authorization 없음 → 401 | PASS |
| `/internal/publish` env 미설정 + Authorization 있음 → 401 | PASS |
| `/internal/publish` env 설정 + Authorization 누락 → 401 | PASS |
| `/internal/publish` env 설정 + 잘못된 토큰 → 401 | PASS |
| `/internal/publish` env 설정 + 정확한 토큰 → 200 | PASS |
| `secrets.compare_digest` timing-safe 비교 사용 | PASS |
| 로그에 토큰 raw 값 0건 (메타 문자열만) | PASS |
| SSE service `/healthz` 200 + JSON 시그니처 | PASS |
| Web API `/healthz` 200 + JSON 시그니처 + SKIP_EXACT | PASS |
| supervisor crash-loop 5분 3회 → degraded 격상 | PASS |
| supervisor crash-loop 진입 후 4번째 start spawn 차단 | PASS |
| reset_crash_loop 후 정상 spawn | PASS |
| probe_healthz 200/timeout/404 분기 | PASS |
| phase54~58 회귀 모두 PASS | PASS |

---

## M2-18 완료 (2026-05-10) — publish 실패 유실 정책 적용

- **변경 (backend)**: `publisher.py`에 `logging.getLogger("publisher")` 추가, `_failure_lock = threading.Lock()`, `_failure_meta = collections.deque(maxlen=50)` 도입. `_record_failure(event, reason, url=None)` 헬퍼가 lock 안에서 카운터·deque 원자 갱신 + `logger.warning("sse_publish_failed", extra={"event","reason","url"})` 호출. **token raw 값 0건**(extra에 토큰 미포함). `get_failure_snapshot()`이 `{"count","last_event","last_reason","last_at"}` 반환. in-process fallback 분기에서도 `wu_broker.publish` try/except → `_record_failure` 호출(고장 isolation). `app.py` `/healthz` 응답에 `sse_publish_failures`, `sse_publish_last_event`, `sse_publish_last_at` 3개 키 추가.
- **변경 (frontend)**: `static/js/realtime.js`에 (b) idle visibility/focus 재조회 트리거 추가 (+42 라인). `IDLE_REFETCH_THRESHOLD_MS=60000`, `MIN_REFETCH_INTERVAL_MS=5000`. `click/keydown/scroll/mousemove` 활동 추적(passive listener). `visibilitychange`/`focus` 이벤트에서 60초+ idle + 5초 가드 + `wuShouldSuppressRefetch()` false 시 `wu:events:changed` 1회 dispatch. 디버그 도구 `window.__wuRealtimeDebug = {lastActivityAt, lastRefetchAt, idleMs}`. (a) 재연결 직후 재조회 동작 보존(line 51-55 `_everConnected` 분기 변경 0건).
- **회귀 테스트 추가**: `tests/phase60_sse_publish_failure_policy.py`(22종 단언). phase59에 `[7d/7e/7f]` 단언 추가(healthz의 새 3개 키 강제 검증, phase59 25/25 PASS).
- **증거**: `_workspace/perf/scripts/m2_18_publish_failure_policy_probe.py` / `_workspace/perf/m2_18_publish_failure_policy/runs/20260509_181930/probe.md`. probe 17/17 PASS, phase60 22/22 PASS.
- **명령**: `D:\Program Files\Python\Python312\python.exe tests\phase60_sse_publish_failure_policy.py` PASS, `D:\Program Files\Python\Python312\python.exe _workspace\perf\scripts\m2_18_publish_failure_policy_probe.py` PASS. phase54(5/5)/55(16/16)/56(15/15)/57(22/22)/58(19/19)/59(25/25) 회귀 모두 PASS.
- **검증 항목**: IPC unreachable 시 `sse_publish_failure` +1 + logger.warning 호출 + extra(`event`/`reason`/`url`) 포함 + token/Authorization 0건 / `get_failure_snapshot()` 시그니처(`count`/`last_event`/`last_reason`/`last_at`) / `/healthz` JSON에 `sse_publish_failures`/`sse_publish_last_event`/`sse_publish_last_at` 키 존재 / `publisher.publish` raise 0건(silent) — 호출자 코드 흐름 정상 / in-process fallback에서도 broker 예외가 isolation됨 / DB rollback 금지(publisher.publish 자체가 silent fail이라 transaction 흔들림 0).
- **Frontend manual smoke (M2-20 라이브 검증 예정)**: ① 60초+ 타 탭 이동 후 복귀 → 핵심 API GET 1회 / ② 60초+ idle 후 alt-tab 복귀 → 동일 / ③ 30초 idle 복귀 → 재조회 미발생(threshold 미충족) / ④ 복귀 직후 5초 안 두 번째 visibility change → 두 번째 미발생(MIN_REFETCH 가드).
- **범위 제한**: Web API 단의 buffer/재시도 큐 미도입(plan §13 명시). admin UI/tray에 카운터 표시는 M2-20. SSE service 강제 종료 → 재시작 사이의 라이브 부하 검증은 M2-20.
- **다음 step 영향**: M2-19 SSE proxy 6종은 본 step과 직접 의존 없음(publish 실패 카운터는 SSE service의 proxy 통과 양과 별개 채널). M2-20에서 `/healthz`의 `sse_publish_failures`를 측정 지표 인덱스의 SSE 누락 채널로 직접 참조 + frontend manual smoke 4종 라이브 회귀.

### M2-18 publish 실패 정책 검증

| 케이스 | 결과 |
|---|---|
| IPC unreachable + sse_publish_failure +1 | PASS |
| logger.warning 호출 + extra(event/reason/url) 포함 | PASS |
| extra/log에 token/Authorization raw 값 0건 | PASS |
| `get_failure_snapshot()` 시그니처 (count/last_event/last_reason/last_at) | PASS |
| `/healthz` JSON에 sse_publish_failures/last_event/last_at 키 존재 | PASS |
| publisher.publish silent (raise 0건) | PASS |
| in-process fallback broker 예외 isolation | PASS |
| frontend (a) 재연결 재조회 보존 (line 51-55 변경 0) | PASS |
| frontend (b) IDLE_REFETCH_THRESHOLD_MS=60000 / MIN=5000 / passive listener | PASS |
| phase54~59 회귀 모두 PASS | PASS |

---

## M2-19 완료 (2026-05-10) — Front Router SSE proxy 6종 조건 적용

- **결정**: 운영 코드 변경 **0건**. ASGI dispatch 구조에서 plan §13 6종 조건이 자연 보장됨을 단언으로 잠금. 결함 발견 0건.
- **변경**: 신규 `tests/phase61_front_router_sse_proxy.py`(31종 단언) + `_workspace/perf/scripts/m2_19_sse_proxy_six_conditions_probe.py`. 운영 코드 `git diff`(app.py/front_router.py/sse_service.py/main.py/supervisor.py/publisher.py/static/js/realtime.js) 출력 0건.
- **6종 조건 보장 근거**:
  1. **buffering 비활성**: SSE service `StreamingResponse`가 chunk를 yield하면 ASGI message가 즉시 client로 forward(별도 buffering middleware 0건). 응답 헤더에 `Cache-Control: no-cache` + `X-Accel-Buffering: no` + `Connection: keep-alive` 모두 포함.
  2. **compression 비활성**: `app.py`/`front_router.py`/`sse_service.py`/`main.py`/`supervisor.py` 5개 파일에 `GZipMiddleware`/`gzip`/`brotli`/`compression` import grep 0건. ASGI 응답 헤더에 `content-encoding: gzip|br` 없음.
  3. **idle timeout 정책**: Front Router의 `FrontRouter.__call__`에 `asyncio.wait_for`/timeout 0건. SSE service `gen()`에 `_last_ping = time.monotonic()` + `time.monotonic() - _last_ping >= 25.0` heartbeat 코드 존재. ASGI dispatch는 timeout 미적용.
  4. **client disconnect 전파**: ASGI in-process composition에서 client disconnect 메시지가 SSE service `request.is_disconnected()` → break → finally `broker.unsubscribe`로 전파. simul: subs 0→1(subscribe)→0(disconnect 후 unsubscribe) 단언 통과.
  5. **헤더/쿠키 통과**: `front_router.py`의 `FORWARDED_HEADER_NAMES`에 `cookie`/`authorization` 미포함. inbound Cookie/Authorization은 strip 대상 아님이라 downstream에 그대로 forward. M2-11 strip-then-set은 X-Forwarded-* 6종만 재작성.
  6. **외부 `/internal/*` 차단**: `match_front_route("/internal/foo")` → `FrontRoute(target="blocked", reason="external_internal_block", blocked=True)`. ASGI 404 응답 + downstream 호출 0건. M2-10 산출물 회귀 잠금.
- **회귀 테스트 추가**: `tests/phase61_front_router_sse_proxy.py`(31종 단언, UTF-8 stdout reconfigure 포함, phase54~60 스타일).
- **증거**: probe 31/31 PASS, phase61 31/31 PASS. `_workspace/perf/m2_19_sse_proxy/runs/20260509T183817Z/probe.md`.
- **명령**: `D:\Program Files\Python\Python312\python.exe tests\phase61_front_router_sse_proxy.py` PASS, `D:\Program Files\Python\Python312\python.exe _workspace\perf\scripts\m2_19_sse_proxy_six_conditions_probe.py` PASS. phase54(5/5)/55(16/16)/56(15/15)/57(22/22)/58(19/19)/59(25/25)/60(22/22) 회귀 모두 PASS.
- **범위 제한**: isolated ASGI level 검증이다. 라이브 브라우저 EventStream 탭의 chunk별 latency, 50 SSE 연결 평균 publish→수신 지연, 외부 `curl https://host/internal/publish` 404/403 응답은 M2-20 정책별 증거 인덱스에서 라이브 회귀로 보강.
- **다음 step 영향**: M2-20에서 본 step의 6종 단언이 회귀 기준선이 된다. 라이브 부하 테스트가 SSE chunk 즉시 forward·publish→수신 지연·disconnect 시 stale subscriber 0를 측정하면 §17 M2 완료 기준 ⑱ Front Router SSE proxy 3종 증거가 충족된다.

### M2-19 SSE proxy 6종 검증

| 조건 | 결과 | 보장 근거 |
|---|---|---|
| 1. buffering 비활성 | PASS | StreamingResponse 헤더 + ASGI 무버퍼링 |
| 2. compression 비활성 | PASS | 5개 파일 grep 0건 + 응답 헤더에 content-encoding 없음 |
| 3. idle timeout 정책 | PASS | FrontRouter asyncio.wait_for 0건 + SSE 25.0초 heartbeat 코드 존재 |
| 4. client disconnect 전파 | PASS | subs 0→1→0 + finally unsubscribe 단언 |
| 5. 헤더/쿠키 통과 | PASS | FORWARDED_HEADER_NAMES cookie/authorization 미포함 + X-Forwarded-* 6종만 재작성 |
| 6. 외부 `/internal/*` 차단 | PASS | match_front_route blocked + 404 + downstream 0건 |
| 운영 코드 변경 0건 | PASS | git diff 출력 없음 |
| phase54~60 회귀 모두 PASS | PASS | regression suite |

---

## M2-20 완료 (2026-05-10) — M2 종료 부하 테스트 + 정책별 증거 인덱스

- **변경**: 운영 코드 변경 0건. `_workspace/perf/scripts/m2_20_live_supervisor_integration_probe.py`, `_workspace/perf/scripts/m2_20_50sse_load_probe.py` 신규 추가. 본 step은 검증·증거 정리·라이브 통합 단언 전용. `git diff --stat -- app.py supervisor.py sse_service.py front_router.py main.py publisher.py auth.py` 출력 없음(운영 코드 변경 0건 확인).
- **증거**: 아래 정책별 증거 인덱스 20종 표 + 라이브 통합 결과 2종.
- **회귀**: phase54(5/5)/55(16/16)/56(15/15)/57(22/22)/58(19/19)/59(25/25)/60(22/22)/61(31/31) 모두 PASS.
- **다음 step 영향**: M2 종료. M3 진입 게이트 평가는 별도 step.

### M2-20 정책별 증거 인덱스 (18종 + 추가 보안 2종)

| ID | 항목 | step | 증거 파일/명령 | 회귀 테스트/probe |
|---|---|---|---|---|
| ① | 외부 포트 소유자: Tray/Supervisor/Front Router | M2-1 | `성능 개선 계획.md` §13, §16 장기 모델 — Tray/Supervisor/Front Router가 8000/8443 소유 결정 | 코드/서버 변경 없음 (결정 step) |
| ② | 사용자-facing SSE 경로: Front Router `/api/stream` → SSE service | M2-2 | `성능 개선 계획.md` §13 옵션(a) 채택, `static/js/realtime.js:45` `new EventSource('/api/stream')` | 코드 변경 없음 (결정 step) |
| ③ | canonical URL: `request.base_url.hostname` 직접 사용 0건 + `/settings/mcp` 외부 URL | M2-3 | `app.py:387` `_public_base_url()` — `rg -n "request\.base_url\|base_url\.hostname" app.py templates static/js` 0건 | `python -m py_compile app.py` PASS |
| ④ | HTTP 8000 unsafe write 차단 정책 + pre/post baseline | M2-4 | `_workspace/perf/baseline_2026-05-09/m2_4_004355_pre_policy/m2_4_http_fallback_probe.md` / `_workspace/perf/baseline_2026-05-09/m2_4_004435_post_policy/m2_4_http_fallback_probe.md` | `_workspace/perf/scripts/m2_4_http_fallback_probe.py` |
| ⑤ | HSTS 미적용 결정 + max-age=0 | M2-5 | `_workspace/perf/baseline_2026-05-09/m2_5_005255_hsts/m2_5_hsts_probe.md` — HSTS absent 확인 | `_workspace/perf/scripts/m2_5_hsts_probe.py` |
| ⑥ | AVR scope-out 아님 (b) 채택 + 회귀 5종 통과 | M2-6 | `_workspace/perf/baseline_2026-05-09/m2_6_005839_avr/m2_6_avr_policy_probe.md` | `_workspace/perf/scripts/m2_6_avr_policy_probe.py` |
| ⑦ | strip-then-set 위조 헤더 3종(XFF/X-Forwarded-Host/X-Forwarded-Proto) 동시 주입 회귀 | M2-11 | `_workspace/perf/front_router_m2_11/runs/20260510_013222/m2_11_forwarded_probe.md` | `_workspace/perf/scripts/m2_11_forwarded_probe.py` |
| ⑧ | SlowAPI limiter `auth.get_client_ip` 통일 + trusted proxy 50 bucket + 위조 무시 + 11번째 429 | M2-12 | `_workspace/perf/limiter_m2_12/runs/20260510_014210/m2_12_limiter_probe.md` | `tests/phase54_limiter_trusted_proxy.py` (5/5 PASS) |
| ⑨ | TRUSTED_PROXY + 외부 직접 차단: loopback-only 403 + `extra_env` override 차단 | M2-13 | `_workspace/perf/trusted_proxy_m2_13/runs/20260510_020446/m2_13_trusted_proxy_probe.md` | `tests/phase55_front_router_trust_boundary.py` (16/16 PASS) |
| ⑩ | CSRF Host 보존 + Origin 위조 차단 | M2-14 | `_workspace/perf/csrf_host_m2_14/runs/20260509T172318Z/m2_14_csrf_probe.md` | `tests/phase56_front_router_csrf_host.py` (15/15 PASS) |
| ⑪ | HTTPS probe middleware 라우터 경유 probe HTML 0 + 외부 직접 8000 회귀 | M2-15 | `_workspace/perf/https_probe_m2_15/runs/20260509T173402Z/m2_15_https_probe_compat.md` | `tests/phase57_https_probe_router_compat.py` (22/22 PASS) |
| ⑫ | Supervisor 상태 표시: pid/health/restart_count/last_error/log path | M2-9, M2-17 | `_workspace/perf/supervisor_m2_9/runs/20260510_011022/m2_9_supervisor_probe.md` / `_workspace/perf/m2_17_health_token_crashloop/runs/20260509T181437Z/probe.md` | `_workspace/perf/scripts/m2_9_supervisor_probe.py` / `tests/phase59_sse_token_health_crashloop.py` (25/25 PASS) |
| ⑬ | crash 1회 자동 재시작 + 5분 3회 crash-loop 차단/degraded | M2-17 | `_workspace/perf/m2_17_health_token_crashloop/runs/20260509T181437Z/probe.md` — crash-loop 5분 3회→degraded, reset→정상 spawn | `tests/phase59_sse_token_health_crashloop.py` (25/25 PASS) |
| ⑭ | `sse_publish_failure` 카운터 + SSE service 종료/재시작 시 카운터 증가 | M2-18 | `_workspace/perf/m2_18_publish_failure_policy/runs/20260509_181930/probe.md` | `tests/phase60_sse_publish_failure_policy.py` (22/22 PASS) |
| ⑮ | DB rollback 금지: publish 실패 시 DB commit 정상 + SSE 재연결 재조회 | M2-18 | `_workspace/perf/m2_18_publish_failure_policy/runs/20260509_181930/probe.md` — `_sse_publish` get_conn 블록 밖 37건 grep PASS | `tests/phase60_sse_publish_failure_policy.py` [6b/6c] PASS |
| ⑯ | PyInstaller frozen self re-spawn 빌드 검증 | M2-8 | `_workspace/perf/pyinstaller_m2_8/runs/20260510_010409/m2_8_pyinstaller_respawn_probe.md` | `_workspace/perf/scripts/m2_8_pyinstaller_respawn_probe.py` |
| ⑰ | internal token 인증: 토큰 없음→401, 잘못된→401, 정상→200 | M2-17 | `_workspace/perf/m2_17_health_token_crashloop/runs/20260509T181437Z/probe.md` + M2-20 라이브: `_workspace/perf/m2_20_live/runs/20260509T184924Z/live_supervisor_integration.md` | `tests/phase59_sse_token_health_crashloop.py` (25/25 PASS) |
| ⑱ | Front Router SSE proxy 6종: buffering/compression/idle/disconnect/header/external internal block | M2-19 | `_workspace/perf/m2_19_sse_proxy/runs/20260509T183817Z/probe.md` | `tests/phase61_front_router_sse_proxy.py` (31/31 PASS) |
| +A | MCP handshake 실패/access log Authorization raw 값 Bearer token 원문 0건 | M2-17, M2-18 | `rg -n "log.*Authorization\|logger.*Authorization\|log.*Bearer\|logger.*Bearer" app.py sse_service.py publisher.py mcp_server.py` 결과 0건 | grep 명령 결과 0건 확인 |
| +B | `/uploads/meetings/*` 비멤버 권한 검사 | M2-10 | `app.py:189` `_ProtectedMeetingStaticFiles.get_response` + `app.py:150` `_can_read_uploaded_file` — permission 함수 사전 존재 + M2-10 routing 경유 도달 확인. 비멤버 deny-path 테스트는 phase39/40_security_patch 계열에 있음 | `_workspace/perf/front_router_m2_10/runs/20260510_012803/m2_10_front_router_probe.md` |

### M2-20 라이브 Supervisor 통합 결과

`_workspace/perf/scripts/m2_20_live_supervisor_integration_probe.py` 실행 결과:

| 단계 | 결과 |
|---|---|
| WhatUdoinSupervisor + ensure_internal_token() | PASS |
| sse_service.py subprocess.Popen spawn (startup_grace 2s) | PASS (status=running) |
| /healthz 폴링 → 200 + status=ok | PASS |
| /internal/publish: Authorization 없음 → 401 | PASS |
| /internal/publish: `Bearer wrong` → 401 | PASS |
| /internal/publish: `Bearer <correct>` + JSON → 200 ok=True | PASS |
| stop_all(): status=stopped + process terminated | PASS |
| token 파일 잔존 (supervisor stop_all은 파일 삭제 안 함) | PASS |
| **총계** | **14/14 PASS** |

증거 파일: `_workspace/perf/m2_20_live/runs/20260509T184924Z/live_supervisor_integration.md`

### M2-20 50 SSE 부하 시뮬레이션 결과

`_workspace/perf/scripts/m2_20_50sse_load_probe.py` 실행 결과 (경과 31.5s, 60s cap 내):

| 항목 | 측정값 | SLA | 판정 |
|---|---|---|---|
| SSE 연결 성공 | 50/50 (219ms) | ≥90% | PASS |
| event 수신 도달 | 50/50 | ≥90% | PASS |
| publish→수신 p95 | <0.1ms (loopback asyncio fan-out) | <2000ms | PASS |
| subscribers=0 복귀 (disconnect 후 2s) | 0 | ==0 | PASS |
| SSE service /healthz 50회 동시 GET p95 | 16.0ms | <500ms | PASS |
| **총계** | | | **10/10 PASS** |

**범위 제한**: Web API /healthz 라이브 spawn 제외 — 60초 cap + SQLite WAL lock 위험 + DB init 부하. SSE service /healthz 50회 동시 요청으로 JSON GET API 회귀 대역 측정함.

증거 파일: `_workspace/perf/m2_20_live/runs/20260509T184932Z/50sse_load_probe.md`

### M2 종료 게이트 평가

| 평가 항목 | 결과 | 근거 |
|---|---|---|
| M2 마일스톤 의도(SSE service 분리) 충족 | PASS | `sse_service.py` 독립 프로세스로 분리, `publisher.py` IPC 추상화, `supervisor.py` lifecycle 관리 |
| §13 진입 게이트 통과 후 적용 항목 19개 회귀 | PASS | phase54~61 모두 PASS (총 155개 단언), M2-20 라이브 통합 14/14 PASS |
| 50 SSE 부하 + Web API 회귀 0건 | PASS | publish→수신 p95=<0.1ms(loopback fan-out), /healthz p95=16ms, 수신 도달 50/50 |
| 라이브 supervisor 통합 | PASS | spawn→healthz→token 3종→stop_all 14/14 PASS |
| M3 진입 게이트 평가 | 별도 step으로 분리 | M2-20 범위 외 |

---

## 분리 4단계 활성화 (2026-05-10) — SSE + Front Router full 분리 운영 모드 통합

- **변경**: `front_router.py`(+211 라인) Starlette + httpx AsyncClient reverse HTTP proxy mode + `__main__` 진입점(env 기반 외부 8000/8443 listener owner). `/internal/*` 차단, `/api/stream` SSE service streaming proxy(text/event-stream chunk forward + idle timeout 비활성 + client disconnect 시 upstream close), 그 외 Web API HTTP forward. M2-11 strip-then-set forwarded headers + M2-14 Host 보존 정책 적용. cert/key 있으면 HTTPS listener도 시작.
- `supervisor.py`(+128): `FRONT_ROUTER_SERVICE_NAME="front-router"` + 6 env 상수, `front_router_service_spec()` factory(http_port/https_port/cert/key/web_api_url/sse_url protected env), `WEB_API_INTERNAL_PORT_ENV`/`WEB_API_INTERNAL_DEFAULT_PORT=8769` + `web_api_internal_runtime_spec()` factory(internal port + internal-only mode). `STOP_ORDER` 6종 갱신 — `(ollama, media, sse, scheduler, web-api, front-router)`.
- `app.py`(+6 라인): direct launcher port를 env 기반(`WHATUDOIN_WEB_API_INTERNAL_PORT`) 분기.
- `main.py`(+119): `WHATUDOIN_ENABLE_FRONTEND_ROUTING=1` env 토글. 활성화 시 supervisor 4-way 공유(`if scheduler or media or ollama or frontend_routing:`) + Web API service spawn(internal port 8769 + internal-only) + SSE service spawn + Front Router service spawn(8000/8443 외부, web_api_url=http://127.0.0.1:8769, sse_url=http://127.0.0.1:8765, cert/key 자동 주입). main.py 자체는 외부 listener 미가동(`servers = []`로 빈 listener), tray + supervisor stop_all만 관리. 미설정 시 기존 fastapi_app 직접 listener 100% 유지.
- **회귀 테스트 추가**: `tests/phase79_sidecar_stage4_frontend.py`(55종 단언). main.py 토글 코드 + supervisor 4-way 공유 + 외부 listener 미가동 분기 + front_router.py reverse proxy 코드(httpx + Starlette + /internal block + strip-then-set + /api/stream streaming) + supervisor.front_router_service_spec smoke + Web API internal runtime spec + STOP_ORDER 6종 + app.py port env 분기.
- **phase70/71/78 갱신**: 4단계 통합으로 STOP_ORDER 5종 → 6종 단언, supervisor 인스턴스 공유 패턴 4-way로 확장. phase76/77/78도 grep 패턴 일관성 확인.
- **증거**: `_workspace/perf/scripts/m2_4b_front_router_full_probe.py`(36종 PASS), `_workspace/perf/scripts/m2_4b_live_routing_probe.py`(15종 PASS, 라이브 4 service spawn → Front Router routing → /healthz/8000/8443 → /api/stream → /internal block → stop_all graceful), 결과 markdown `_workspace/perf/m2_4b_full_routing/runs/20260510T072610Z/full_routing_probe.md`.
- **명령**: `python -c "import main, front_router, supervisor, app; print(OK)"` PASS, `D:\Program Files\Python\Python312\python.exe tests\phase79_sidecar_stage4_frontend.py` 55/55 PASS, `m2_4b_front_router_full_probe.py` 36/36 PASS, `m2_4b_live_routing_probe.py` 15/15 PASS. phase54~79 26개 회귀 모두 PASS.
- **운영 모드**:
  - **fallback (default, env 미설정)**: 기존 main.py가 외부 8000/8443 직접 listener owner. fastapi_app 직접 띄움. 1·2·3단계 sidecar 토글은 그대로 동작.
  - **4단계 활성화 (`WHATUDOIN_ENABLE_FRONTEND_ROUTING=1`)**: supervisor가 Front Router(8000/8443 외부 listener owner) + Web API(127.0.0.1:8769 internal-only) + SSE service(127.0.0.1:8765) 셋 spawn. 외부 사용자 트래픽이 Front Router → Web API/SSE로 reverse proxy. 외부 직접 internal port 접근은 차단(M2-13).
- **알려진 한계** (backend-dev 보고):
  1. SSE upstream status 코드 — reverse proxy가 SSE service의 첫 응답 status를 그대로 forward하지만 일부 edge case에서 미세한 timing 차이 가능. 라이브 검증에서는 문제 없음.
  2. 4-B 단독 활성화 시 1·2·3단계 sidecar(scheduler/media/ollama)는 자동으로 켜지지 않음 — 별도로 토글해야 함. 4 토글 모두 활성화 시 6 service 동시 spawn 가능.
- **운영 가이드**:
  - **활성화**: `$env:WHATUDOIN_ENABLE_FRONTEND_ROUTING="1"; python main.py`
  - **모든 분리 활성화**: 4 토글 모두 설정 + python main.py — 6 service spawn(Front Router + Web API + SSE + Scheduler + Ollama + Media)
  - **degraded 복구**: Front Router crash → supervisor crash-loop 차단(M2-17). 5분 3회 임계 초과 시 status=degraded. 외부 사용자 connection refused. 토글을 끄고 main.py 재시작하면 fallback으로 복귀.
- **다음 단계**: 4단계까지 완료. 모든 sidecar 분리 활성화 가능. 운영 안정화 + 측정 후 §18 후속 후보(보수 단축안/MCP write tool/DB command service) 별도 사이클로 처리.
