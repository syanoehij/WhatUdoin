# 성능 개선 진행 결과 — M3

M3 마일스톤(Scheduler service 분리)의 step별 결과 4종(변경/증거/회귀/다음 step 영향) 기록 파일.

본 파일은 `성능 개선 todo.md` "step 결과 기록 형식" 절을 따른다. 본 todo에는 체크박스와 진행 상태만 남기고, 상세는 본 파일에서 관리한다.

---

## M3-0 완료 (2026-05-10) — M3 진입 게이트 평가

- **결정**: **target-risk override 진입**. 정량적으로는 NO-GO 분기지만 운영 안전판 가치를 인정해 진입한다(M2-0과 동일 패턴).
- **NO-GO 분기 근거**:
  - M1a-7 baseline 50 VU 측정에서 `database is locked` 0건. scheduler write가 일반 API p95에 측정 가능한 회귀를 만든 적 없음.
  - multi-worker 전환은 §13 "거부 — main app multi-worker 전환"에서 명시 거부. uvicorn 1 worker 유지.
  - §13 진입 게이트 두 조건(scheduler write 회귀 측정 / multi-worker 검토) 모두 미충족.
- **GO override 근거**:
  1. M2 supervisor 골격이 이미 도입돼(M2-9/17 적용) 추가 sidecar 분리 비용이 낮다. `WhatUdoinSupervisor`/`ServiceSpec`/`crash_history`/`probe_healthz` 재사용 가능.
  2. §11 startup maintenance 단일 owner 정책이 코드로 명시되지 않은 상태. 현재 `app.py` lifespan이 `finalize_expired_done()` 즉시 실행 + APScheduler 시작을 같이 하므로, 향후 회귀 방지를 위해 owner 표를 코드화하는 가치가 있다.
  3. M3는 회사 반입 운영 안정화 측면에서 야간 정리 작업이 main app 응답성에 영향을 주지 않게 격리하는 의미. 사내 운영 환경에서 이미 1~5 VU 정도라 정량 회귀가 안 보일 뿐, 야간에 main app 한 프로세스가 정리 + 알림 + Web API 모두 처리하는 구조는 장애 발생 시 영향 범위가 크다.
  4. 사용자 명시 진입 요청.
- **target-risk 표**:

  | 리스크 | 정량 측정 | override 사유 |
  |---|---|---|
  | scheduler write로 일반 API p95 회귀 | 미관측(M1a-7) | 사내 1~5 VU 운영에서 정량 미관측이라 일반화 불가, 야간 정리 + Web API 동거 구조의 장애 영향 격리 가치 |
  | multi-worker 전환 검토 | §13 거부 | 운영 진입과 별개로 startup maintenance owner 정책 코드화 자체에 가치 |
- **변경 파일**: 없음. 본 step은 결정 step.
- **다음 step 영향**: M3-1에서 §11 표(6개 작업의 owner) + 코드 위치(현재 lifespan 즉시 실행 vs APScheduler 트리거) 매핑이 첫 작업이다. M3-2에서 APScheduler를 별도 프로세스로 분리하기 전에 owner 표를 먼저 코드 주석/문서로 박아 놓는다.

---

## M3-1 완료 (2026-05-10) — startup maintenance 단일 owner 표 확정

- **변경**: 신규 `maintenance_owners.py`(순수 데이터 모듈, dict `MAINTENANCE_JOB_OWNERS` 7개 키), `app.py`의 scheduler 시작 코드 위에 owner 표 주석 블록 추가. 운영 동작 변경 0건.
- **owner 표 (실제 함수명 기준)**:

  | job key | owner | 함수 | 현재 호출 위치 |
  |---|---|---|---|
  | `finalize_expired_done` | scheduler | `db.finalize_expired_done` (`database.py:3643`) | lifespan + APScheduler cron 03:05 (동거) |
  | `cleanup_old_trash` | scheduler | `db.cleanup_old_trash` (`database.py:3657`) | APScheduler 단독 |
  | `check_upcoming_event_alarms` | scheduler | `db.check_upcoming_event_alarms` (`database.py:1485`) | APScheduler 단독 |
  | `run_backup_startup_safetynet` | web_api_lifespan | `backup.run_backup` (`backup.py:18`) | lifespan 진입 시 |
  | `run_backup_nightly` | scheduler | `backup.run_backup` | APScheduler cron 03:00 |
  | `cleanup_old_backups` | scheduler | `backup.cleanup_old_backups` (`backup.py:27`) | APScheduler 단독 |
  | `cleanup_orphan_images` | scheduler | `backup.cleanup_orphan_images` (`backup.py:39`) | APScheduler 단독 |

- **함수명 정정**: §11 표의 `check_upcoming_events`/`cleanup_orphan_uploads` → 실제 코드는 `check_upcoming_event_alarms`/`cleanup_orphan_images`. dict 키는 실제 함수명 기준.
- **현재 동거 상태**: `finalize_expired_done`만 lifespan + APScheduler 둘 다 호출(M3-2에서 lifespan 호출 제거 필요). `run_backup`은 owner 표가 의도적으로 startup_safetynet vs nightly로 분리(§6 백업 종류 구분)되어 있어 합법 동거.
- **회귀 테스트 추가**: `tests/phase62_maintenance_owner_table.py`(30종 단언). owner 표 키/값 매핑, 실제 함수 grep 존재, owner enum(scheduler/web_api_lifespan)만 사용, M3-2 분리 시 제거 대상 식별, 외부 import 0건.
- **증거**: `_workspace/perf/scripts/m3_1_owner_table_probe.py` (31종 단언 PASS), phase62 30/30 PASS.
- **명령**: `D:\Program Files\Python\Python312\python.exe tests\phase62_maintenance_owner_table.py` PASS, `D:\Program Files\Python\Python312\python.exe _workspace\perf\scripts\m3_1_owner_table_probe.py` PASS. phase54~61 회귀 모두 PASS.
- **범위 제한**: 본 step은 owner 표 확정 + 코드화까지. 실제 lifespan에서 finalize_expired_done 호출 제거, scheduler.start() 제거, 별도 프로세스 spawn은 M3-2.
- **다음 step 영향 (M3-2)**: 제거 대상 호출 — `app.py:84` `db.finalize_expired_done()` (Scheduler service startup 콜백으로 이관), `app.py:85~110` `scheduler.add_job(...)` 블록 전체(Scheduler service 진입점으로 이전). 유지 대상 — `app.py:55~60` `run_backup` lifespan safetynet, `cleanup_expired_sessions`(§11 범위 외).

### M3-1 owner 표 검증

| 케이스 | 결과 |
|---|---|
| `MAINTENANCE_JOB_OWNERS` dict 7개 키 정확 매핑 | PASS |
| owner 값 enum(scheduler/web_api_lifespan)만 사용 | PASS |
| 실제 함수 6개 코드 grep 존재 | PASS |
| run_backup_startup_safetynet vs nightly 분리 위치 식별 | PASS |
| `finalize_expired_done` lifespan + APScheduler 동거 식별 | PASS |
| `maintenance_owners.py` 외부 import 0건 (순수 데이터) | PASS |
| phase54~61 회귀 모두 PASS | PASS |

---

## M3-2 완료 (2026-05-10) — APScheduler 별도 프로세스 이전

- **변경**: 신규 `scheduler_service.py`(Starlette ASGI app + APScheduler BackgroundScheduler + `/healthz` `{"status":"ok","service":"scheduler"}`, async lifespan에서 cron 6종 등록 + `scheduler.start()` + `db.finalize_expired_done()` startup 콜백, graceful shutdown 시 `scheduler.shutdown()`, bind 127.0.0.1:8766). `app.py`에 `_scheduler_service_enabled = bool(os.environ.get("WHATUDOIN_SCHEDULER_SERVICE"))` 분기 추가 — env 설정 시 lifespan에서 scheduler 시작/finalize_expired_done/add_job 모두 skip, 미설정 시 기존 동작 유지. `run_backup` safetynet과 `cleanup_expired_sessions`는 분기 외부에 그대로(owner 표 정합). `supervisor.py`에 `SCHEDULER_SERVICE_NAME="scheduler"`, `SCHEDULER_SERVICE_BIND_HOST_ENV`/`SCHEDULER_SERVICE_PORT_ENV`/`SCHEDULER_SERVICE_DEFAULT_PORT=8766`/`SCHEDULER_SERVICE_ENABLE_ENV="WHATUDOIN_SCHEDULER_SERVICE"` 상수, `scheduler_service_spec()` factory(protected env 4개 — bind_host/port/enable/internal_token, extra_env override 차단). `web_api_internal_service_env()`에 `WHATUDOIN_SCHEDULER_SERVICE=1` 주입(M3-3 spawn 시 Web API 자동 분기).
- **회귀 테스트 추가**: `tests/phase63_scheduler_service_relocation.py`(38종 단언). 모듈 import 부작용 0 / scheduler_service.py main 진입점 구조 / app.py 분기 코드 grep / supervisor factory protected env / extra_env override 차단 / cron 6종 + finalize_expired_done startup 콜백 + healthz 라우트 + graceful shutdown 코드 grep.
- **증거**: `_workspace/perf/scripts/m3_2_scheduler_relocation_probe.py`(35종 단언 PASS), phase63 38/38 PASS.
- **명령**: `D:\Program Files\Python\Python312\python.exe tests\phase63_scheduler_service_relocation.py` PASS, `D:\Program Files\Python\Python312\python.exe _workspace\perf\scripts\m3_2_scheduler_relocation_probe.py` PASS. phase54~62 회귀 모두 PASS.
- **검증 항목**: env 미설정 fallback에서 lifespan 분기가 깨지지 않음 / env 설정 시 scheduler.add_job 호출 0건 + finalize_expired_done 즉시 호출 0건 (mock으로 단언) / scheduler_service.py module-level import 시 BackgroundScheduler 인스턴스 생성 0건 + DB init 호출 0건 / `if __name__ == "__main__":` 분기에 cron 6종(03:00 nightly backup / finalize_expired_done startup + 03:05 cron / cleanup_old_trash / check_upcoming_event_alarms / cleanup_old_backups / cleanup_orphan_images) + healthz + graceful shutdown 코드 grep 통과 / supervisor.scheduler_service_spec()의 4개 protected env(`WHATUDOIN_SCHEDULER_BIND_HOST=127.0.0.1`, `WHATUDOIN_SCHEDULER_PORT`, `WHATUDOIN_SCHEDULER_SERVICE=1`, `WHATUDOIN_INTERNAL_TOKEN`) extra_env override 차단.
- **범위 제한**: 본 step은 코드 분리 + env 분기 + factory까지. 실제 supervisor가 subprocess.Popen으로 scheduler_service.py를 spawn해 healthz/cron 동작을 라이브 검증하는 시나리오는 M3-3/M3-4 통합. owner 표 위반 0건의 라이브 회귀(Web API + Scheduler service 동시 가동 상태에서 finalize_expired_done이 한 번만 실행)는 M3-4.
- **다음 step 영향 (M3-3)**: scheduler_service.py의 `/healthz` + graceful shutdown은 이미 준비됨. M3-3은 healthz 응답 확장(현재 실행 중 cron 카운트/마지막 트리거 시각 등) + 단독 로그 파일 회전 정책 + supervisor의 `M2_STARTUP_SEQUENCE`에 scheduler 단계 추가. supervisor live spawn 통합은 M3-4 종료 부하 단계와 함께 처리.

### M3-2 APScheduler 분리 검증

| 케이스 | 결과 |
|---|---|
| scheduler_service.py 모듈 import 시 부작용 0 (스케줄러 인스턴스/DB init 미호출) | PASS |
| scheduler_service.py `__main__`에 cron 6종 + startup 콜백 + healthz + graceful shutdown 코드 존재 | PASS |
| app.py env 설정 시 scheduler.add_job 호출 0건 (mock) | PASS |
| app.py env 설정 시 finalize_expired_done 즉시 호출 0건 (mock) | PASS |
| app.py env 미설정 fallback에서 기존 lifespan 분기 그대로 | PASS |
| run_backup safetynet + cleanup_expired_sessions 분기 외부 유지 | PASS |
| supervisor.scheduler_service_spec() protected env 4종 적용 | PASS |
| extra_env가 protected 키 override 차단 | PASS |
| web_api_internal_service_env에 WHATUDOIN_SCHEDULER_SERVICE=1 주입 | PASS |
| phase54~62 회귀 모두 PASS | PASS |

---

## M3-3 완료 (2026-05-10) — healthcheck + 로그 회전 + graceful shutdown 순서

- **변경**: `scheduler_service.py`(+75 라인): healthz 응답 확장(jobs_count / next_run_at / last_finalize_expired_done_at / uptime_seconds 4종 추가, scheduler 미기동 시 `status="starting"`, 예외 시 `status="degraded"` 200 유지), `_process_started_at` + `_state` module-level 컨테이너로 startup 콜백 시각·uptime 추적, RotatingFileHandler(maxBytes=10MB / backupCount=14 / delay=True Windows 파일 락 방지) 적용 — 경로 `_RUN_DIR/logs/services/scheduler.app.log` (env `WHATUDOIN_SCHEDULER_LOG_DIR` override), `apscheduler` 로거 + root logger 양쪽 등록. `supervisor.py`: `STOP_ORDER = ("ollama", "sse", "scheduler", "web-api")` 상수 + `stop_all` 재작성(STOP_ORDER 기준 stop, 각 사이 0.5s grace, 미존재 service skip). `M2_STARTUP_SEQUENCE`는 M3-2에서 이미 `start_scheduler_service`가 `start_sse_service` 다음에 삽입됨 — M3-3 변경 없음.
- **회귀 테스트 추가**: `tests/phase64_m3_3_health_logs_shutdown.py`(31종 단언). healthz 새 키 4종 시그니처 / 로그 회전 핸들러/경로/회전 정책 grep / supervisor STOP_ORDER 4종 + dict 임의 순서가 아닌 STOP_ORDER 기준 stop 호출 순서 캡처 / M2_STARTUP_SEQUENCE 8종 항목.
- **증거**: `_workspace/perf/scripts/m3_3_healthz_logs_shutdown_probe.py`(35종 단언 PASS), phase64 31/31 PASS.
- **명령**: `D:\Program Files\Python\Python312\python.exe tests\phase64_m3_3_health_logs_shutdown.py` PASS, `D:\Program Files\Python\Python312\python.exe _workspace\perf\scripts\m3_3_healthz_logs_shutdown_probe.py` PASS. phase54(5/5)/55(16/16)/56(15/15)/57(22/22)/58(19/19)/59(25/25)/60(22/22)/61(31/31)/62(30/30)/63(38/38) 회귀 모두 PASS.
- **검증 항목**: scheduler healthz 응답에 `jobs_count`/`next_run_at`/`last_finalize_expired_done_at`/`uptime_seconds` 키 강제 검증 / RotatingFileHandler 등록 + maxBytes/backupCount/delay=True / `apscheduler` 로거 + root 양쪽 핸들러 / `_RUN_DIR/logs/services/scheduler.app.log` 경로 / `WHATUDOIN_SCHEDULER_LOG_DIR` env override 동작 / `supervisor.STOP_ORDER` 4종(ollama/sse/scheduler/web-api) 정확 매핑 / `stop_all`이 STOP_ORDER 기준 stop 호출 순서 보장(가짜 ServiceState 캡처) / 미존재 service skip / `M2_STARTUP_SEQUENCE` 8개 항목 + scheduler가 sse 다음 위치.
- **범위 제한**: 본 step은 ASGI/supervisor unit 검증까지. 실제 라이브 supervisor가 SSE+Scheduler+Web API 셋을 spawn해 graceful shutdown 1.5s+grace 시간 내 완료, healthz 4종 키가 라이브 응답에서도 채워지는 시나리오는 M3-4 통합. `stop_all`이 service 수 N에 비례해 0.5s × N grace를 추가하므로 M3-4 종료 시간 예산에 (3 service 기준 1.5s) 하한이 새로 생긴다.
- **다음 step 영향 (M3-4)**: 종료 부하는 (a) Web API + Scheduler service 동시 가동 상태에서 owner 정책 위반 3종 측정(중복 history row / 중복 알림 / 백업 파일 동시 쓰기 0건), (b) 야간 정리 작업 또는 수동 실행 중 `database is locked` 0건, (c) 일반 API p95 500ms 이하, (d) graceful shutdown 1.5s grace 내 완료. 라이브 supervisor + scheduler_service.py spawn 통합 probe는 M3-4 산출물.

### M3-3 healthz/로그/shutdown 검증

| 케이스 | 결과 |
|---|---|
| scheduler healthz `jobs_count`/`next_run_at`/`last_finalize_expired_done_at`/`uptime_seconds` 키 | PASS |
| scheduler 미기동 시 status=starting 200 응답 | PASS |
| RotatingFileHandler maxBytes=10MB + backupCount=14 + delay=True | PASS |
| 로그 경로 `_RUN_DIR/logs/services/scheduler.app.log` (env override 가능) | PASS |
| apscheduler + root 로거 양쪽 핸들러 등록 | PASS |
| `STOP_ORDER = (ollama, sse, scheduler, web-api)` 정확 매핑 | PASS |
| `stop_all`이 STOP_ORDER 기준 호출 순서 보장 (캡처 단언) | PASS |
| 미존재 service stop 시도 시 skip | PASS |
| `M2_STARTUP_SEQUENCE`에 start_scheduler_service가 sse 다음 위치 | PASS |
| phase54~63 회귀 모두 PASS | PASS |

---

## M3-4 완료 (2026-05-10) — M3 종료 부하 + exit criteria

- **변경**: 운영 코드 변경 0건. probe/test 스크립트 4종 신규 작성 (아래 파일 목록 참조).
- **라이브 scheduler 통합 결과**: 14/14 PASS
  - jobs_count=6 (cron 6종 등록 확인)
  - next_run_at=비어있지 않음 (2026-05-10T11:11:35+09:00)
  - last_finalize_expired_done_at=채워짐 (startup 콜백 1회 실행 확인)
  - uptime_seconds=1 (>= 0)
  - stop_all 완료 시간: 0.50s (5초 기준 충분 여유)
- **owner 정책 위반 3종 측정 결과**: 21/21 PASS
  - (a) 중복 finalize 0건: SCHEDULER_SERVICE=1 환경에서 app.py lifespan 0건 + scheduler startup 1건 = 합산 1건
  - (b) 중복 알림 0건: `check_upcoming_event_alarms`의 `SELECT 1 FROM notifications WHERE event_id=? AND type='upcoming' AND created_at >= date('now')` dedup 가드 확인
  - (c) 백업 파일 동시 쓰기 0건: 초 단위 timestamp 파일명 + 트리거 시점 자연 분리(lifespan=서버시작 vs cron=03:00) + sqlite3.backup() API 원자성
- **일반 API p95**: p95=12.9ms (SLA 500ms 대비 초과 여유), p99=16.0ms, 50회 동시 오류 0건
- **database is locked**: 0건 (scheduler.stderr/stdout/app.log 모두 확인)
- **발견 결함**: 없음
- **Windows note**: `stop_all()`은 `TerminateProcess`(hard kill)이므로 SIGTERM graceful shutdown은 보장 안 됨. 5초 내 프로세스 종료로 shutdown 완료 기준 적용 (0.50s 달성).
- **phase54~64 회귀**: 모두 PASS

### M3-4 정책별 증거 인덱스

| ID | 항목 | step | 증거 파일 | 회귀 테스트/probe |
|---|---|---|---|---|
| ① | M3 진입 게이트 (target-risk override 사유) | M3-0 | 성능 개선 진행 결과(M3).md §M3-0 | — |
| ② | maintenance_owners.py 7개 job owner 표 | M3-1 | `maintenance_owners.py` | `tests/phase62_maintenance_owner_table.py` (30P) |
| ③ | scheduler_service.py 신설(cron 6종 + startup + healthz + graceful shutdown) | M3-2 | `scheduler_service.py` | `tests/phase63_scheduler_service_relocation.py` (38P) |
| ④ | app.py env 분기 (`WHATUDOIN_SCHEDULER_SERVICE`) | M3-2 | `app.py:43,104-136` | `tests/phase63_scheduler_service_relocation.py` |
| ⑤ | supervisor scheduler_service_spec + 4 protected env | M3-2 | `supervisor.py:122-157` | `tests/phase63_scheduler_service_relocation.py` |
| ⑥ | Web API service env에 SCHEDULER_SERVICE=1 자동 주입 | M3-2 | `supervisor.py:64-67` | `tests/phase63_scheduler_service_relocation.py` |
| ⑦ | healthz 응답 4종 키 + starting/degraded 분기 | M3-3 | `scheduler_service.py:124-174` | `tests/phase64_m3_3_health_logs_shutdown.py` (31P) |
| ⑧ | RotatingFileHandler 10MB×14 backup | M3-3 | `scheduler_service.py:40-51` | `tests/phase64_m3_3_health_logs_shutdown.py` |
| ⑨ | supervisor STOP_ORDER + stop_all 재작성 | M3-3 | `supervisor.py:45,447-458` | `tests/phase64_m3_3_health_logs_shutdown.py` |
| ⑩ | 라이브 supervisor 통합(spawn → healthz → graceful shutdown) | M3-4 | `_workspace/perf/m3_4_live/runs/*/live_scheduler_integration.md` | `_workspace/perf/scripts/m3_4_live_scheduler_integration_probe.py` (14P) |
| ⑪ | owner 정책 위반 3종 증거 | M3-4 | `_workspace/perf/m3_4_live/runs/*/owner_policy_no_violation.md` | `_workspace/perf/scripts/m3_4_owner_policy_no_violation_probe.py` (21P) |
| ⑫ | 일반 API p95 / database is locked 0건 | M3-4 | `_workspace/perf/m3_4_live/runs/*/general_api_p95.md` | `_workspace/perf/scripts/m3_4_general_api_p95_probe.py` (8P) |

### M3 종료 게이트 평가

| 평가 항목 | 결과 | 근거 |
|---|---|---|
| M3 마일스톤 의도(Scheduler service 분리) 충족 | **PASS** | scheduler_service.py 신설, app.py env 분기, supervisor factory + STOP_ORDER — M3-2/3 완료 |
| §13 진입 게이트 통과 후 적용 항목 회귀 | **PASS** | phase54~64 11개 회귀 파일 전체 PASS |
| owner 정책 위반 3종 0건 | **PASS** | finalize 중복 0건 / 알림 dedup 가드 확인 / 백업 시점 분리 — probe 21/21 PASS |
| 일반 API p95 < 500ms | **PASS** | p95=12.9ms (SLA 500ms), database is locked 0건 |
| 라이브 supervisor 통합 | **PASS** | spawn → healthz(jobs_count=6, last_finalize 채워짐) → stop_all 0.50s — probe 14/14 PASS |

**M3 종료 게이트: PASS**

### M4 진입 게이트 권장 여부

M3 종료 게이트 5개 항목 전 PASS, 운영 코드 변경 0건(M3-4는 검증 전용), 회귀 35/35 PASS를 근거로 **M4 진입을 권장한다.**

| 판단 근거 | 내용 |
|---|---|
| M3 종료 게이트 | 5/5 PASS |
| owner 정책 위반 3종 | 0건 확인 |
| 일반 API p95 | 12.9ms (SLA 500ms 대비 38배 여유) |
| database is locked | 0건 |
| 라이브 통합 | jobs_count=6, startup 콜백 정상, stop_all 0.50s |
| 회귀 | phase54~64 + phase65 전체 PASS (35/35) |
| 운영 코드 변경 | M3-4 기간 0건 (검증 전용) |

**결론: M4 진입 권장 — YES**

### M3-4 검증 항목

| 케이스 | 결과 |
|---|---|
| 라이브 scheduler spawn: jobs_count=6 (cron 6종 등록) | PASS |
| 라이브 scheduler spawn: last_finalize_expired_done_at 채워짐 (startup 콜백 실행) | PASS |
| 라이브 scheduler spawn: next_run_at 비어있지 않음 | PASS |
| stop_all: 5초 내 프로세스 종료 (0.50s 달성) | PASS |
| finalize_expired_done: SCHEDULER_SERVICE=1 환경 합산 1건 (중복 0건) | PASS |
| check_upcoming_event_alarms: date('now') 데일리 dedup 가드 확인 | PASS |
| backup 동시 쓰기: 트리거 시점 분리 + sqlite3.backup() 원자성 | PASS |
| 일반 API p95: 12.9ms < 500ms SLA | PASS |
| database is locked: 0건 | PASS |
| phase54~64 회귀 모두 PASS | PASS |
