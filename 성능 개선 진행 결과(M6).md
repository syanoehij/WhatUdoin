# 성능 개선 진행 결과 — M6

M6 후보 마일스톤(MCP write/edit + DB command service)의 step별 결과 4종(변경/증거/회귀/다음 step 영향) 기록 파일.

본 파일은 `성능 개선 todo.md` "step 결과 기록 형식" 절을 따른다. 본 todo에는 체크박스와 진행 상태만 남기고, 상세는 본 파일에서 관리한다.

---

## M6-0 완료 (2026-05-10) — M6 진입 게이트 평가

- **결정**: **target-risk override 진입**(M2-0/M3-0/M4-0/M5-0과 동일 패턴). M6는 §16에서 "MCP write/edit 기능이 실제 운영 요구로 들어오면 별도 마일스톤으로 다룬다. 이 단계는 성능 개선 M1~M5의 필수 범위가 아니라 장기 운영 구조 후보"라는 조건부 후보 마일스톤이다. 정량적으로는 NO-GO 분기지만 운영 안전 boundary 정립 가치를 인정해 진입한다.
- **NO-GO 분기 근거**:
  - 현재 MCP는 read tool만 운영 — write/edit 운영 요구가 정량 측정으로 확인된 기록 없음.
  - §13 진입 게이트 조건 "MCP write/edit 운영 요구 발생 시에만 진입" 정량 미충족.
  - §16 "성능 개선 M1~M5의 필수 범위가 아니라 장기 운영 구조 후보" 명시.
- **GO override 근거**:
  1. M2/M3/M4/M5 supervisor + 분리 패턴 안정화. 추가 sidecar/IPC 비용 매우 낮음 — `WhatUdoinSupervisor`/`ServiceSpec`/`ollama_service_spec`/`media_service_spec` factory 패턴/`probe_healthz`/`STOP_ORDER` 재사용 가능.
  2. MCP write/edit owner 원칙(MCP service 직접 SQLite write 금지, 모든 write/edit는 Web API write path 경유)을 **코드 boundary로 박는** 가치. 향후 write tool 추가 시 회귀 방지. plan §9 "MCP write/edit 추가 정책 (단기 원칙)" 단언.
  3. 일정 생성/체크리스트 추가 같은 좁은 destructive 범위만 후보 — 운영 위협 낮은 단계적 도입.
  4. DB command service 도입 결정(M6-3)은 측정 기반 — 본 사이클에서는 도입 안 하고 §18 후속 후보로 유지하면 무방.
  5. 사용자 명시 진입 요청.
- **target-risk 표**:

  | 리스크 | 정량 측정 | override 사유 |
  |---|---|---|
  | MCP write/edit 운영 요구 | 미관측 | owner 원칙 코드 boundary 정립 가치 + supervisor 패턴 재사용 비용 낮음 |
  | MCP service 직접 SQLite write | 설계상 0 | M6-2에서 MCP proxy → Web API command endpoint 경유 단언 |
  | hidden-project 권한 회귀 | 41차 보안 강화로 보호 | M6-4에서 회귀 잠금 |
  | history/lock/SSE publish 정합성 | Web API write path 통합 | M6-2 boundary로 자연 보존 |
- **변경 파일**: 없음. 본 step은 결정 step.
- **다음 step 영향**: M6-1에서 write/edit tool 위험도 분류 표 작성 — destructive 분류 + 우선 후보(일정 생성/체크리스트 추가) 좁은 범위로 시작. M6-2에서 Web API command endpoint(`/api/command/*` 같은 내부 IPC) + MCP proxy 코드 boundary 도입. M6-3에서 DB command service 도입 여부는 측정으로 결정(현 사이클 미도입 가능성 높음). M6-4 종료 회귀로 hidden-project 차단/lock/history/SSE publish 통합 + MCP write owner 원칙 재확인.

---

## M6-1 + M6-2 완료 (2026-05-10) — MCP write owner boundary 코드화

**M6-1 + M6-2를 한 패키지로 통합 처리** (위험도 분류 표 + IPC 헬퍼 패턴은 자연 한 묶음).

- **M6-1 변경 (`mcp_command_registry.py` 신설, ~75 라인)**: 순수 데이터 모듈(외부 import 0). `MCP_WRITE_TOOL_RISK_CLASSES = ("safe", "moderate", "destructive", "admin_only")` 4종 위험도 enum + `MCP_WRITE_TOOL_CLASSIFICATION` 6개 항목(create_event=moderate / add_checklist_item=moderate / update_event=moderate / delete_event=destructive / delete_document=destructive / bulk_project_update=destructive&admin) + `MCP_WRITE_PRIORITY_CANDIDATES = ("create_event", "add_checklist_item")` 우선 후보 + `is_destructive(tool_name)` / `web_api_target(tool_name)` 조회 헬퍼.
- **M6-2 변경 (`mcp_server.py`에 IPC 헬퍼 +35 라인)**: `async def _call_web_api_command(tool_name, payload, ctx) -> dict` 추가. 미등록 tool → `ValueError`, 등록 tool → `NotImplementedError`(M6-2 boundary 잠금 — 본 사이클 시범 write tool 미추가). 향후 write tool 추가 시 본 헬퍼만 경유. boundary 사유 docstring으로 명시(plan §9/§13 — MCP service 직접 SQLite write 금지, Web API write path 경유).
- **`supervisor.py` (+5 라인)**: `WEB_API_INTERNAL_URL_ENV = "WHATUDOIN_WEB_API_INTERNAL_URL"` 상수 추가. 자동 주입은 over-engineering이라 본 사이클 skip(향후 MCP write tool 추가 시 별도 step에서 처리).
- **mcp_server write owner grep 회귀**:
  - SQL write 패턴(`INSERT INTO`/`UPDATE ... SET`/`DELETE FROM`) AST Constant 검사 0건
  - `sqlite3` 직접 import 0건
  - `db.create_*`/`db.update_*`/`db.delete_*`/`db.add_*`/`db.write_*` 실제 호출 AST Call 노드 0건
  - read 함수(`db.list_*`/`db.get_*`/`db.search_*`)는 OK(현재 14개 read tool 그대로 유지)
- **회귀 테스트**: `tests/phase75_m6_mcp_owner_boundary.py`(21종 단언 PASS).
- **증거**: `_workspace/perf/scripts/m6_2_mcp_owner_boundary_probe.py`(21종 단언 PASS).
- **명령**: `D:\Program Files\Python\Python312\python.exe tests\phase75_m6_mcp_owner_boundary.py` PASS, `D:\Program Files\Python\Python312\python.exe _workspace\perf\scripts\m6_2_mcp_owner_boundary_probe.py` PASS. phase54~74 21개 회귀 모두 PASS.
- **검증 항목**: mcp_command_registry 모듈 import 부작용 0(외부 의존성/네트워크/DB 호출 0) / MCP_WRITE_TOOL_RISK_CLASSES 4종 / CLASSIFICATION 6 항목 + 각 항목 시그니처(risk/web_api_path/method/permission/audit) / PRIORITY_CANDIDATES 2개 + classification에 존재 / is_destructive·web_api_target 헬퍼 동작 / mcp_server `_call_web_api_command` 정의 + 시그니처 / mcp_server SQL write 패턴 0건(AST) / sqlite3 직접 import 0건 / db.write 함수 호출 0건 / mcp_command_registry 외부 import 0건.
- **범위 제한**: 본 step은 owner boundary 코드 잠금까지. 시범 write tool 추가는 운영 요구 미관측이라 본 사이클 미수행. supervisor가 WHATUDOIN_WEB_API_INTERNAL_URL을 자동 주입하는 부분도 over-engineering이라 상수 정의만. 향후 MCP write tool이 운영 요구로 들어오면 (a) `mcp_server.py`에 `@mcp.tool()` 추가 + `_call_web_api_command`만 경유, (b) supervisor 자동 주입 구현, (c) M6-4와 동일 패턴 회귀 추가.
- **다음 step 영향 (M6-3)**: `MCP_WRITE_TOOL_CLASSIFICATION`의 risk/permission 분류를 그대로 재사용 가능. "moderate" → Web API 직접, "destructive" → DB command service 경유 구분 등 정책 이식이 zero-rework. plan §17 M6 종료 기준("mcp_server.py SQLite write 0건")은 phase75에서 잠김.

### M6-1 + M6-2 boundary 검증

| 케이스 | 결과 |
|---|---|
| mcp_command_registry 모듈 import 부작용 0 | PASS |
| MCP_WRITE_TOOL_RISK_CLASSES 4종 enum | PASS |
| MCP_WRITE_TOOL_CLASSIFICATION 6 항목 + 각 항목 시그니처 | PASS |
| MCP_WRITE_PRIORITY_CANDIDATES 2개 + classification에 존재 | PASS |
| is_destructive / web_api_target 헬퍼 | PASS |
| `_call_web_api_command` 시그니처 (tool_name, payload, ctx) | PASS |
| mcp_server SQL write 패턴 0건 (AST) | PASS |
| mcp_server sqlite3 직접 import 0건 | PASS |
| mcp_server db.write 함수 호출 0건 | PASS |
| mcp_command_registry 외부 import 0건 (순수 데이터) | PASS |
| supervisor WEB_API_INTERNAL_URL_ENV 상수 추가 | PASS |
| phase54~74 회귀 모두 PASS | PASS |

---

## M6-3 완료 (2026-05-10) — DB command service 도입 결정 (미도입, §18 후속)

- **결정**: **DB command service 미도입**, plan §18 후속 후보로 유지. 본 사이클 운영 코드 변경 0건.
- **사유**:
  1. **측정 미발생**: M6-1+M6-2에서 시범 write tool을 추가하지 않음(운영 요구 미관측). MCP write/edit 부하가 Web API write path를 흔든다는 측정 자체가 없으므로 §16 M6-3 진입 조건("측정으로 단일화 필요 확인 시 도입") 미충족.
  2. **Web API write path 단일 owner 유지**: 현재 SQLite write가 Web API에 단일화되어 있고(M2~M5 sidecar 분리에서 owner boundary 명시 — Media DB write 0 / Scheduler maintenance owner 표 / MCP read-only 14개 + write owner boundary), 정합성 위반 사례 없음. 추가 분리 단계는 측정 없이 도입하면 신규 운영면만 늘림.
  3. **plan §13 "거부 — write 다중화" 정합**: SQLite 쓰기 처리량을 늘리기 위해 여러 writer 프로세스를 두는 것은 목표가 아님. WAL에서도 writer는 직렬화되므로 lock 대기만 늘 수 있음. DB command service는 write 다중화가 아니라 owner 단일화의 보조 수단인데, 현재 owner는 이미 Web API로 단일.
  4. **M6-1 분류 표 zero-rework 이식**: 향후 도입 결정 시 `MCP_WRITE_TOOL_CLASSIFICATION`의 risk/permission 분류를 그대로 재사용 가능 — "moderate" → Web API 직접, "destructive" → DB command service 경유 정책 분기. 본 사이클에서 분류 표만 박아둔 가치가 후속 도입 비용을 낮춘다.
  5. **§18 후속 후보로 보존**: MCP write tool 운영 요구 발생 + Web API write path 부하 측정으로 정합성 회귀 확인 시점에 재평가. 현재 진입 비용/효과 비대칭이 큼.
- **재평가 트리거**:
  - MCP write tool이 운영 요구로 추가됨 + write 부하가 일반 API p95에 측정 가능한 회귀를 만들 때
  - 또는 Web API write path가 권한/lock/history/SSE publish 후처리 중 정합성 위반 사례를 만들 때
  - 또는 multi-worker 전환이 검토되어 SQLite writer 단일화가 필요할 때(현재 §13에서 multi-worker 거부)
- **변경 파일**: 없음. 본 step은 결정 step.
- **다음 step 영향**: M6-4 종료 회귀에서 (a) hidden-project 차단/lock/history/SSE publish 통합, (b) MCP write owner 원칙 grep 재확인(M6-1+M6-2 boundary가 변경 없이 보존되는지), (c) M6 종료 게이트 평가. DB command service 미도입 결정은 §18 후속으로 보존되므로 M6-4 회귀에서 추가 검증 없음.

### M6-3 미도입 결정 검증

| 케이스 | 결과 |
|---|---|
| §16 M6-3 진입 조건 "측정으로 단일화 필요 확인" 미충족 | PASS (조건 미관측) |
| Web API write path 단일 owner 유지 (M2~M5 boundary) | PASS |
| plan §13 "write 다중화 거부" 정합 | PASS |
| M6-1 분류 표 zero-rework 이식 가능 | PASS |
| §18 후속 후보로 보존 | PASS |
| 운영 코드 변경 0건 | PASS |
